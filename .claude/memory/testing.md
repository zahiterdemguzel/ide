# Testing & lint

The app has no automated GUI/end-to-end harness, but the **pure, subtle logic** — the parts most likely to break silently under an edit — is split out into Electron-free modules and unit-tested with Node's built-in runner. There is **no test-framework dependency**: `npm test` is `node --test` with a tiny custom reporter.

## Running

- `npm test` — runs every file under `test/` (`node --test`'s default glob), through `scripts/test-reporter.js`: a quiet reporter that stays silent for passing tests and prints only failures (name + stack) plus a one-line `N passed, M failed` summary, so a green run is one line and a regression is the only thing you read.
- `npm run lint` — ESLint (flat config in `eslint.config.js`) over the whole tree.

There is no separate lint/test CI job — run `npm test` and `npm run lint` locally before finishing a change. The only GitHub Actions workflow is `.github/workflows/build.yml`, which packages the Windows, macOS, and Linux apps on every push to `master` (see below).

## Build workflow

`.github/workflows/build.yml` runs on every push to `master` (and manual `workflow_dispatch`). It has two jobs:

- **`build`** — a three-OS matrix that packages the app with electron-builder: `windows-latest` runs `npm run build` (NSIS installer `.exe`), `macos-latest` runs `npm run build:mac` (`.dmg`), and `ubuntu-latest` runs `npm run build:linux` (`.AppImage` — runs on any distro without a package manager). Each job uploads its installer as a workflow artifact (`windows` / `macos` / `linux`). `CSC_IDENTITY_AUTO_DISCOVERY=false` keeps electron-builder from attempting code-signing/notarization, since CI has no certificates. `fail-fast: false` lets each OS finish even if another breaks.
- **`release`** — runs after all builds, downloads their artifacts, and publishes them to a single rolling GitHub Release tagged `latest` (via `softprops/action-gh-release`). Each push refreshes that release, so the newest `.exe` + `.dmg` + `.AppImage` are always one click from the repo's front page under **Releases**. It needs `contents: write` permission and is gated on `github.event_name == 'push'` (a `workflow_dispatch` dry run skips it, since there's no commit to tag). The release is marked `prerelease` because the binaries are unsigned.

`package-lock.json` is gitignored, so the repo has no lockfile in it. That means the workflow uses `npm install` (not `npm ci`) and omits setup-node's `cache: npm` — both of those require a committed lockfile and would fail with "Dependencies lock file is not found."

## The testability split

A module that does `require('electron')` (or reads files, spawns git, touches the DOM) can't be loaded under plain Node. So the rule is: **keep the OS/IPC shell thin and put the real logic in an Electron-free helper next to it.** This mirrors the existing `edit-ops.js` (pure) ↔ `session-commit.js` (IPC) split.

| Helper (pure, tested) | Shell (IPC/IO, untested) | What's covered |
|---|---|---|
| `src/main/edit-ops.js` | `src/main/session-commit.js` | `editOp`/`replayEdits`/`inverseEdits` — turning file-tool calls into replayable ops and replaying / de-applying them (the per-session commit/revert core, including the cross-session "don't clobber the other session's edits" guarantee). Also `diffStat` — added/removed line counts for a session row's "+124 −38" pill, counted from the ops because a working-tree diff can't attribute lines to a session. Its approximations (a `write` has no pre-image, so its removals are invisible) are asserted, not incidental — keep them that way. |
| `src/main/commit-msg.js` | `src/main/git.js`, `src/main/session-commit.js` | `commitMessagePrompt` (Haiku prompt for a diff, capped), `cleanCommitMessage` (strip ``` fences / wrapping quotes, trim, cap) and `fallbackCommitMessage` (session title → first-prompt line → id stub) — the shared phrasing/cleanup both the main git pane and the per-session commit use to author a message from a diff via Haiku. The IPC shells run `git diff`/`runHaiku` and feed the strings here. |
| `src/main/concurrency.js` | `src/main/session-commit.js` | `createLimiter(max)` — the FIFO concurrency limiter the `session-diff-stat` handler runs through, so a burst of per-session diff-stat requests (startup, tab switch, git refresh — each several git spawns) runs a few at a time instead of all at once. Tested for the max-in-flight cap, FIFO order, per-task results, and that a rejection/sync-throw frees the slot. |
| `src/main/companion-files.js` | `src/main/session-commit.js` | `companionPaths` — the Godot sidecar naming (`<file>.uid`, `<file>.import`) a per-session commit folds in alongside each resource, and the rule that a sidecar has no sidecar of its own. session-commit.js does the disk reads / HEAD blob-hash comparison around it. |
| `src/main/db-sql.js` | `src/main/db.js` | The [database viewer](architecture.md#database-viewer)'s pure core: `isSqliteBuffer` (the 16-byte header sniff), the `DB_ENGINES` catalog + `engineForFile`, `quoteIdent`/`quoteLiteral`, `rowIdentity` (rowid → pk → all-columns), the parameterized `buildSelect`/`buildCount`/`buildUpdate`/`buildInsert`/`buildDelete` (LIMIT/OFFSET integer-coerced), `coerceInput` (SQLite type-affinity coercion, empty → NULL), and the `isReadOnlySql` heuristic. db.js owns the sql.js (WASM SQLite) database + filesystem IO + `db-*` IPC. |
| `src/main/run-configs-lib.js` | `src/main/run-configs.js` | `parseJsonc` (comments, trailing commas, comment-like text inside strings), `listRunConfigs` (toolbar entries: hidden filtering, `presentation.order`, name dedupe), and the launch/task → shell-command translation (`buildLaunchCommand`, `buildTaskCommand`, `resolveTask`, `chainCommands`). Platform-specific behaviour (PowerShell `$?` chaining vs `&&`) is tested by **injecting** the platform into `makeRunConfigLib(repoPath, platform)`. |
| `src/main/runners-lib.js` | `src/main/runners.js` | `langForFile` (extension → language, case-insensitive, non-runnable types) and `buildRunCommand` (interpreter + file → quoted command line, incl. `go run`/`powershell -File`/`deno run` shapes and appended args) — the [run-a-file](architecture.md#run-a-file) translation. runners.js owns PATH probing, the interpreter store, and IPC. |
| `src/main/pty-batch.js` | `src/main/sessions.js`, `src/main/consoles.js` | `createPtyBatcher` — buffers per-id PTY output and flushes the concatenation once per frame, coalescing many tiny `onData` chunks into one IPC message (order preserved; `flushId` drains the tail and cancels the timer). Timer functions are **injected** so the batching is tested deterministically with a manual scheduler. The shells own the real PTY + `sendToRenderer`. |
| `src/main/git-parse.js` | `src/main/git.js`, `src/main/session-commit.js` | `parsePorcelain` (staged/unstaged/conflicts split, rename arrows, unmerged states, non-ASCII paths), `parseLog` (unit-separator field splitting), `markPushed`/`markIncoming` (tagging history rows as unpushed/incoming without mutating the input), `filterCommits` (History-search term matching across subject/author/hash), and `sumNumstat` (totals a `git diff --numstat` into additions/deletions/files for the per-session Diff badge, counting a binary file as a changed file with 0 lines). git.js / session-commit.js run the subprocess and feed stdout here. |
| `src/main/repo-create.js` | `src/main/git.js` | `validateRepoName` (GitHub name rules — non-empty, no `.`/`..`, only letters/digits/`-`/`_`/`.`) and `ghCreateArgs` (assembles the `gh repo create` argv, public/private flag, description omitted when blank) for the non-git folder's [create-repository panel](architecture.md#create-a-repository). git.js runs `git init`/`commit` and the `gh` subprocess around these. |
| `src/main/search-ignore.js` | `src/main/explorer.js` | `shouldSkipDir` — which dirs the explorer's filename walk (`search-names`/`list-files`) skips: named dependency/build dirs across ecosystems (`node_modules`, `venv`, `target`, `dist`, …) plus any un-allowlisted dot-dir — and `GREP_EXCLUDE_PATHSPECS`, the matching `git grep` excludes for the references search (`search-refs`). Tested for the dot-dir catch-all/allowlist split and that the grep pathspecs stay pure-exclude (a positive pathspec would silently flip grep into search-only-these mode). |
| `src/main/recent-folders.js` | `src/main/repo.js` | `addRecent` — front-insert + dedup + cap for the Open-folder recent-projects list (the reverse-combobox). repo.js handles the file persistence and IPC. |
| `src/main/remote-config-lib.js` | `src/main/remote.js` | `normalizeConfig` — the persisted [remote-access](remote-access.md) service state (`{enabled, port}`) read back off disk: a missing/corrupt file is off-on-the-default-port, `enabled` is strict (no truthy opt-in), and an invalid port falls back to the fixed `DEFAULT_PORT` rather than to `0` — which the ws server reads as "any ephemeral port", the moving address that used to strand every paired phone on restart. remote.js owns the file IO, the listen/fallback, and the auto-start on `whenReady`. |
| `server/debug.js` | `server/relay.js`, `hub.js`, `relay-client.js`, `http-proxy.js` | The backend's [`IDE_DEBUG` operation trace](remote-access.md#debug-tracing-ide_debug): `parseScopes`/`scopeEnabled` (comma list, `1`/`true`/`*` wildcards, and the later-wins `-` negation that makes `*,-tunnel` "everything but the per-chunk byte traffic"), `redact` (blanks any credential-*named* field at any depth — the traced frames carry the pair/device tokens — caps long strings, summarizes buffers) and `formatLine` (absent fields omitted, strings quoted only when they need it). `createDebug` takes an injected env/sink/clock, so what gets printed is asserted without a socket, a console or a real clock. The four socket modules just call the loggers it hands out. |
| `src/main/native-recent-menu.js` | `src/main/native-recent.js` | `jumpListCategories`/`dockMenuItems`/`folderLabel` — the menu structures for the [native recent-projects menus](architecture.md#native-recent-projects-menus) (Windows Jump List / macOS Dock menu). native-recent.js does the Electron `setJumpList`/`dock.setMenu` wiring and ready-gating. |
| `src/main/cli-args.js` | `src/main/repo.js` | `parseFolderArg` — the `--folder <path>` / `--folder=<path>` (alias `--dir`) override that makes a launched instance start with a given directory already open — otherwise no project opens by default (both flag forms, first-occurrence-wins, missing/empty value, non-array argv). repo.js reads `process.argv`, validates the path exists, and applies it without writing back to the recent list. |
| `src/main/slash-parse.js` | `src/main/session-cmd-parse.js` | `feedLine(buf, data, onLine)` — the shared per-session PTY-input line buffer the slash-command watcher layers on (printable bytes accumulate, backspace edits, Enter closes the line via `onLine` + resets, Ctrl-C/U clear, escape sequences are swallowed). Keeps the buffer bookkeeping single-sourced. |
| `src/main/session-cmd-parse.js` | `src/main/sessions.js` | `feedSessionCommand(buf, data)` — detects a `/model <id>` or `/effort <level>` slash command typed straight into the chat (only an exact command line matches, so prose never false-positives) and pushes `session-model` / `session-effort` so the [model badge](settings.md#agent-models) and the phone's chat track a change made at the terminal. Both commands read off **one** buffer: the user is typing one line. |
| `src/main/agent-effort.js` | `src/main/sessions.js` | `effortArgs(effort)` — a session's [reasoning effort](settings.md#reasoning-effort-per-session) as the CLI spawn flag (`--effort <level>`), re-applied on every spawn so a resumed session keeps it. Covers that `auto`/empty adds no flag, and that an **unknown level is dropped rather than forwarded** — unlike a model alias, a bad `--effort` value is a hard CLI error that would leave the session unable to spawn at all. |
| `src/main/ollama-translate-lib.js` | `src/main/ollama-proxy.js` | The Anthropic⇄Ollama-shape translation for [custom models](custom-models.md): `anthropicToOllama` (system-block flatten, `tool_use`/`tool_result` role mapping, tools→function schema, options), `ollamaChunkToAnthropicEvents` (an Ollama-shaped chunk → the exact ordered Anthropic SSE events, incl. `input_json_delta` for tool calls, with open-block `state`), `ollamaDoneToStopReason` (stop/length/tool_use), `nonStreamToAnthropic`. The chunks now come from the in-process node-llama-cpp engine (not an HTTP sidecar); the proxy is only socket plumbing around these. |
| `src/main/ollama-models-lib.js` | `src/main/ollama.js`, `src/renderer/settings.js` | Id namespacing (`isOllamaId`/`toOllamaId`/`ollamaName` — `ollama:<name>`, collision-safe vs `opus`/`sonnet`), `mergeModels` (Claude first, Ollama sorted+deduped), `parsePullProgress` (byte totals→clamped percent, divide-by-zero + malformed guarded), the `CATALOG` (every entry carries numeric `minRam`/`minVram` **and a GGUF `source` URI**, `name` filename-safe) + `catalogFilter`, and `resolvePullTarget`/`deriveName` (map a catalog name or raw `hf:`/URL to `{source, name}`). |
| `src/main/ollama-fit-lib.js` | `src/main/ollama.js`, `src/renderer/custom-models.js` | `modelFit(model, sys)` — the RAM/VRAM fit level (`ok`/`tight`/`fail`/`unknown`) the warning sign is drawn from: hard-fail over budget, `tight` within the headroom margin, VRAM-unknown → RAM-only fallback, Apple-Silicon unified-memory case; plus `formatReq`. The real hardware probe (`detectSystem`) lives in `llama-engine.js` (node-llama-cpp's `getVramState`). |
| `src/main/llama-engine-lib.js` | `src/main/llama-engine.js` | Pure request/response shaping between the Ollama `/api/chat` shape and node-llama-cpp's `LlamaChat`: `buildHistory` (fold each `{role:'tool'}` result back into its assistant turn as a `functionCall` with `result`; append a trailing empty model slot for generation), `buildFunctions` (tool defs → `{name:{description,params}}`, undefined when none), `buildGenOptions` (temperature/top_p/num_predict/stop → LlamaChat options), `toToolCalls` (functionCalls → Ollama tool_calls), `doneReason`, and the weak-model tool-call rescue `looksLikeToolCallStart` / `salvageToolCall` (a whole-reply JSON object naming a known tool → a real tool_call). `llama-engine.js` is the native shell (model load/LRU, prompt-sized context, streaming, downloads). |
| `src/main/proc-env.js` | `src/main/sessions.js`, `src/main/consoles.js` | `cleanEnv(env)` — scrubs VS Code/Electron debugger pollution (`ELECTRON_RUN_AS_NODE`, `VSCODE_INSPECTOR_OPTIONS`, `VSCODE_PID`, and the js-debug `--require`/`--inspect*` entries in `NODE_OPTIONS`) out of an inherited env before any `pty.spawn`, so a spawned `claude` (a Node process — the session PTY *and* the [setup gate](architecture.md#claude-code-setup-gate)'s install/auth terminal) doesn't boot debug-attached and fail. See [platform-notes.md](platform-notes.md). |
| `src/main/claude-install.js` | `src/main/claude.js`, `src/main/sessions.js` | `installGuide(platform)` — the per-platform Claude Code setup shell **argv** for the [setup wizard](architecture.md#claude-code-setup-gate): `installArgs()` (POSIX `['-ilc', …]` login-shell installer / Windows `-NoExit -Command`, with the `OK`/`FAIL` markers keyed on the installer's exit status so a failed install is never reported as success) and `authArgs()` (runs `claude` with the PATH-refresh per OS). `claude.js`'s `claudeAvailable()` runs the actual `claude --version` probe and sessions.js wires the `check-claude` IPC. |
| `src/main/usage-parse.js` | `src/main/claude.js`, `src/main/sessions.js` | `parseUsageHeaders` (the unified rate-limit response headers → `{windows, representative}`: both rolling windows, utilization clamped to 0..1, `five_hour`/`seven_day` → `5h`/`7d`, null when the headers are absent), `resetMs` (epoch-seconds or ISO reset value → ms), `formatResetShort` (ms-until-reset → compact `24m`/`13h`/`2d`/`now`, sub-minute rounded up), and `usageView` (the assembled renderer model with reset tokens + bottleneck flag) — the [usage meter](architecture.md#usage-meter). `claude.js`'s `readUsage()` makes the live Messages API call and sessions.js wires the `get-usage` IPC. |
| `src/main/crashlog-lib.js` | `src/main/crashlog.js` | `crashLogName` (sortable, filesystem-safe `crash-<iso>.log` with no `:`/`.`) and `formatCrash` (kind + time + stack, falling back to `String()` when there's no stack) — the [crash log](architecture.md#error-handling--crash-logs) naming/body. crashlog.js owns the fs writes and the global crash-handler wiring. |
| `src/main/clipboard-lib.js` | `src/main/explorer.js` | `withClipboardRetry(op, opts)` — retries a synchronous clipboard op that throws transiently (Windows "OpenClipboard failed" while another process holds the clipboard lock), returning a fallback after a few short-delayed attempts instead of rejecting. Tested with an injectable `sleep` (no real timers): first-try success, retry-then-succeed, give-up-to-fallback, function-fallback-gets-the-error, and the sleep-between-but-not-after-last-attempt count. explorer.js owns the actual `clipboard.*` calls + the IPC handlers; this keeps the [terminal paste](architecture.md#sessions--git-pane-renderer) reliable. |
| `src/main/hook-events.js` | `src/main/hook-server.js` | `eventToState` (the hook-event → status-dot mapping behind [status detection](status-detection.md), incl. the `git push` → `pushed` command sniff and SessionStart → idle), `shouldApplyState` (a resume's SessionStart → idle must not downgrade an already-meaningful colour), and `hooksSettings(port, statusLineCommand)` (the `claude --settings` JSON wiring every tracked event to a curl POST, and — when given — injecting the per-session [token meter](architecture.md#per-session-token-meter)'s statusLine). hook-server.js runs the http server, holds the live port, and calls into sessions. |
| `src/main/statusline-lib.js` | `src/main/statusline-script.js` | `sumTranscriptTokens` (totals all four token classes across every assistant call in a transcript JSONL — the cumulative session token count), `formatTokens`/`formatCost` (compact `1.23M` / `$0.0421`), and `formatStatusLine` (`model · $cost · tokens` — cost before tokens so edge-truncation hits the token count, not the dollar; right-aligning the metrics to a given `$COLUMNS` width with mid-string padding, falling back to an inline join when width is absent/too small) — the [per-session token meter](architecture.md#per-session-token-meter). statusline-script.js is the thin stdin/fs shell Claude runs; statusline.js stages it on disk. |
| `src/main/transcript-lib.js` | `src/main/chat.js` | `feed`/`applyEntry` — Claude Code's JSONL transcript → the chat messages a phone renders (see [remote-access.md](remote-access.md#the-session-as-a-chat-the-phone-has-no-terminal)). Incremental, because the file is append-only and tailed: a line torn across two reads is held until it's whole, and a **tool result patches the message that carries its call** (they're different lines) rather than becoming a turn of its own. Also `toolTitle` (a call's one-line summary, paths relativized to the project), `userText` (a slash command reads as the line the user typed; the CLI's `<system-reminder>` machinery isn't conversation), the bounded 400-message window, and that an inlined base64 image is reduced to a marker. chat.js owns the fs watch + the byte offset. |
| `src/main/ask-lib.js` | `src/main/chat.js`, `src/main/sessions.js` | `fromHook` — the question a session is blocked on, read off the **hook payload that announces it** (never off the TUI, which cannot be read: see [remote-access.md](remote-access.md#the-session-as-a-chat-the-phone-has-no-terminal)) — and `keystrokes`, the keys that answer it. Tested against payloads **captured from a live CLI**, not invented: an `AskUserQuestion` `PreToolUse` with two questions, and a `PermissionRequest`. Covers that picking auto-advances so N questions answer as N keys + a Submit key, that a custom answer is the extra option + the words + Enter, that a **half-answered box yields no keystrokes at all** (the submit key would otherwise pick whatever sits under the cursor of a question still asking), and that a permission box offers only Yes (`1`) and No (Esc) — never a guessed number for "allow all", which would grant what was declined. |
| `src/main/slash-commands-lib.js` | `src/main/chat.js` | `commandName` (a file under `.claude/commands/` → the name the CLI namespaces it with, `git/sync.md` → `/git:sync`), `commandDescription` (front matter wins, first line of prose is the fallback), and `mergeCommands` (project shadows user shadows builtin, sorted) — the `/` menu in the phone's chat composer. chat.js walks the two directories. |
| `src/renderer/shared/notify.js` | `src/renderer/sessions.js`, `src/renderer/settings.js` | `isCompletionTransition` (fires the finish chime/animation only on working → completed, not on needs-input or an already-settled state) and the `SOUNDS` registry shape (four unique ids, each with a name and well-formed oscillator voices) — the [finish notification](status-detection.md#finish-notification). `notify.js`'s `playNotification`/`getSound`/`setSound` are the browser-only Web Audio + localStorage shell around the pure check. |
| `src/renderer/shared/git-status.js` | `src/renderer/git-pane.js` | `statusLabel` — the human-readable tooltip for a status badge, keyed on the staged/unstaged column so the same porcelain letter (notably `D`: staged deletion vs deleted-on-disk-not-staged) reads distinctly and an unstaged deletion is pointed at Discard. |
| `src/renderer/shared/text-fold.js` | `src/renderer/shared/find.js`, `src/renderer/shared/fuzzy.js`, `src/renderer/viewer/file.js` (and mirrored inline in `src/main/explorer.js`) | `fold` — the shared case/Unicode fold used by every search surface (explorer search, Ctrl+F, Ctrl+P) so non-ASCII text (ş/Ş, ç/Ç, ö/Ö, ü/Ü, İ/I…) matches case-insensitively. Length-preserving: a match offset in the folded string is a valid offset into the original, so the editor's highlights/selection don't drift even past a "İ" (which `toLowerCase` would expand to two code units). |
| `src/renderer/shared/fuzzy.js` | `src/renderer/quick-open.js` | `fuzzyMatch`/`fuzzyFilter` — the Quick Open palette's subsequence matcher: subsequence requirement (case/Unicode-folded via `text-fold.js`, positions stay aligned to the original), consecutive-run / boundary / basename scoring, and the resulting ranking. quick-open.js is the thin DOM/IPC shell around it. |
| `src/renderer/shared/command-match.js` | `src/renderer/command-palette.js` | `matchCommands` — the [Command Palette](architecture.md#command-palette)'s ranker over `{id,title,keywords}`: title hits (highlightable, ranked by `fuzzyMatch` score) before keyword-only fallbacks (penalized, unhighlighted), empty-query passthrough in registration order, limit, and dropping non-matches. command-palette.js is the thin DOM/i18n shell around it. |
| `src/renderer/shared/find.js` | `src/renderer/viewer/file.js` | `findMatches` (all non-overlapping hits, case-(in)sensitive via `text-fold.js`), `nearestMatch` (first hit at/after the caret, wrapping), `stepMatch` (next/prev with wraparound) — the file editor's [find-in-file](architecture.md#file-editor) bar. file.js paints the hits into the `.editor-hl` overlay and drives scrolling. |
| `src/renderer/shared/json-edit.js` | `src/renderer/viewer/file.js` | `jsonInsertion(text, offset)` — the [JSON structure actions](architecture.md#file-editor) edit builder: innermost-container scan (string/escape-aware, null outside or in an unclosed container), append of a `""` element / `"key": ""` entry matching the container's inline-vs-multiline layout and indent (tab/space detection, trailing-comma tolerance), and the placeholder selection range. file.js is the floating-pill shell that applies it via `insertText`. |
| `src/renderer/shared/terminal-links-parse.js` | `src/renderer/terminal-links.js` | `findTerminalLinks` (non-overlapping URL-first-then-path spans in one terminal line) and `looksLikePath` (a token is a path if it has a separator, or is a bare filename with a known extension) — the Ctrl+click link detection that deliberately over-matches then filters. terminal-links.js is the xterm/DOM shell: live decorations, hover tracking, and routing a click to the file viewer or inline browser. |
| `src/renderer/shared/ext.js` | — | `extOf` (lowercased extension after the last dot, `''` when none) and `fileColor` (per-extension tree filename colour, `var(--fg)` fallback), plus the `IMG_EXT`/`AUDIO_EXT`/`MODEL_EXT`/`VECTOR_EXT`/`SCENE_EXT` asset-viewer sets (and the `EDITABLE_MODEL_EXT`/`EDITABLE_VECTOR_EXT` write-back subsets), the `SHEET_EXT` spreadsheet-viewer set, and the `DB_EXT` database-viewer set — the shared file-type helpers used across the tree, file viewer, and terminal links. The asset sets are asserted disjoint (one routing per type), so a regression like leaving `svg` in `IMG_EXT` while adding it to `VECTOR_EXT` fails the test. |
| `src/renderer/shared/svg-ops.js` | `src/renderer/viewer/asset/vector-editor.js`, `vector-view.js` | The [vector editor](architecture.md#vector-editor)'s pure geometry/text helpers: `parseSvgSize`/`applySvgSize`/`ensureSvgXmlns` (read & re-stamp the root `<svg>` size on save, guarantee `xmlns`), `aiKind`/`aiInfo` (`.ai` magic-byte sniff → PDF/PostScript + size/creator for the info card), the z-order array moves (`bringToFront`/`sendToBack`/`forwardOne`/`backwardOne`/`moveItem`), `alignOffsets` (align/distribute deltas), and `snapPoint`/`zoomToward` (grid snap + zoom-toward-cursor math). The editor wires these to the live paper.js document. |
| `src/renderer/shared/pdf-ops.js` | `src/renderer/viewer/asset/pdf-editor.js` | `makePages`/`rotatePage`/`deletePage`/`movePage`/`isIdentity` — the [PDF page editor](architecture.md#pdf-viewer--page-editor)'s immutable page-list model (`{src, rotation}` descriptors): rotation normalized to 0..270, last-page delete and out-of-range ops are no-ops returning the same array, `isIdentity` drives the dirty/Save state. pdf-editor.js is the thumbnail/DOM shell that applies the list with pdf-lib on save. |
| `src/renderer/shared/base64.js` | `src/renderer/viewer/asset/` editors | `base64ToArrayBuffer`/`arrayBufferToBase64` (chunked, stack-safe binary ↔ base64 for the model/pixel editors) and `base64ToText`/`textToBase64` (UTF-8-safe text ↔ base64 for the SVG round-trip — a plain `btoa` would corrupt non-Latin1 `<text>`). Round-trips asserted over ASCII, multibyte (emoji, Turkish), and a real SVG string. |
| `src/renderer/shared/file-icons.js` | `src/renderer/shared/file-icon-img.js` | `iconForFile`/`iconForFolder` (filename/extension → icon id) backing the explorer's per-row [file-type icons](architecture.md#file-explorer). The test also reads `src/renderer/file-icons/` and asserts the mapping and the on-disk SVG set match exactly (every id has a file, every file is referenced) so the two never drift. file-icon-img.js is the thin DOM shell that turns an id into an `<img>`. |
| `src/renderer/shared/sheet-model.js` | `src/renderer/viewer/sheet/` | `colToLetter`/`letterToCol`, `parseA1`/`toA1`, `parseRange` — the [spreadsheet viewer](architecture.md#spreadsheet-viewer)'s 0-based address math (A1 ↔ index, absolute-`$` anchors, normalized ranges). Used by both the grid and the formula engine. |
| `src/renderer/shared/sheet-formula.js` | `src/renderer/viewer/sheet/index.js` | `evaluateFormula` — the dependency-free formula engine (tokenizer → recursive-descent parser → evaluator): operator precedence, refs/ranges, the common worksheet functions, and Excel error values. The grid supplies a `getCell` ctx with memoization + cycle detection; this module is the pure language core. |
| `src/renderer/shared/ico-parse.js` | `src/renderer/viewer/asset/ico-view.js` | `parseIco` (ICONDIR + entry parsing for the [icon viewer](architecture.md#asset-viewer): sizes with the 0-means-256 rule, PNG frames sized from their IHDR, `.cur` type detection, throws on non-ICO/truncated bytes), `extractFrame` (displayable bytes+MIME per frame: PNG streams pass through as `image/png` — Chromium's ICO decoder rejects >256px PNG frames — while BMP DIBs get the `buildSingleIco` one-image .ico wrapper), and `describeEntry` (the "32×32 · 32-bit · PNG" card label). ico-view.js is the DOM shell (frame grid, blob URLs, zoom handoff). |
| `src/renderer/shared/pixel-ops.js` | `src/renderer/viewer/asset/pixel-editor.js` | `floodFill` — the [pixel editor](architecture.md#asset-viewer)'s Fill tool: a 4-connected flood over the `ImageData` byte buffer that recolours (or erases) every pixel within a per-channel threshold of the seed, with the seed-already-filled and out-of-bounds-seed no-ops. pixel-editor.js owns the canvas/pointer shell and hands the buffer here. |
| `src/renderer/shared/adjust-ops.js` | `src/renderer/viewer/asset/image-adjust.js` | `buildChannelLuts` (exposure/brightness/contrast/temperature/tint → one 256-entry per-channel LUT, identity at neutral) and `applyAdjustments` (LUT + per-pixel saturation/vibrance over an RGBA buffer, alpha preserved) — the pixel pipeline behind the [image adjustment view](architecture.md#asset-viewer). image-adjust.js is the canvas/DOM shell (live preview, sliders, `write-asset` save). |
| `src/renderer/shared/tscn.js` | `src/renderer/viewer/asset/scene-editor.js` | The [Godot scene editor](architecture.md#asset-viewer)'s pure `.tscn` model: `parseTscn`/`serializeTscn` (section + raw-property round-trip, multi-line values, Godot's ext_resource spacing), `transformOfNode`/`setNodeTransform`/`serializeTransform3d` (the 12 Transform3D numbers, incl. composing split position/rotation(YXZ)/quaternion/scale), the node-tree ops (`addNode` depth-first placement, `removeNodeTree` + stale connections, `reparentNode` subtree move with path/connection rebasing, `uniqueChildName` Godot-style numbering), `addSubResource`/`addExtResource` (unique ids, type+path dedup, `load_steps` upkeep, typeless `instance=` nodes), and the value helpers (`parseNums`, `parseRef`, `fmtNum`, quote/unquote). scene-editor.js is the three.js/DOM shell over it. |
| `src/renderer/shared/scene-assets.js` | `src/renderer/viewer/asset/scene-editor.js` | The [Godot scene editor](architecture.md#asset-viewer)'s resources-panel model over the flat `list-files` listing: `godotRootOf` (nearest ancestor `project.godot` → the `res://` root, null without one), `modelEntries` (project 3D models with display name, `res://` path, and a **pre-existing** sibling-image thumbnail or null — never generated; models outside the project excluded), and `nodeNameFor` (filename → legal Godot node name). scene-editor.js owns the card strip, drag-and-drop, and the drop raycast. |
| `src/renderer/shared/osc52.js` | `src/renderer/shared/terminal.js` | `decodeOsc52` — decode an OSC 52 clipboard-set payload (`Pc;Pd`) to its UTF-8 text, declining a read (`?`) / empty / malformed request. `attachClipboard()` registers it as the terminal's OSC 52 handler so an app's own copy (e.g. the Claude CLI's mouse-drag "auto copy") reaches the OS clipboard. |
| `src/renderer/shared/name-match.js` | `src/renderer/sessions.js` | `compileQuery`/`matchesTerms`/`matchesQuery` — the [archived-session search](architecture.md#renderer)'s term matching, mirroring the explorer's `search-names`: fold (via `text-fold.js`) + whitespace-split terms that all must match, each a `.ext` suffix or substring. sessions.js wires it to the `#session-search` box and filters the visible archived rows. |
| `src/renderer/shared/session-cycle.js` | `src/renderer/sessions.js` | `nextSessionId` — the Shift+↓ / Shift+↑ [session cycling](onboarding.md#keyboard-cheat-sheet)'s wrap-around math over the visible row order: next/previous with both-ends wrap, jump to an end when the active id isn't visible, and null (no-op) for an empty/single-row list. sessions.js reads the row order off the DOM and wires the keydown handler. |
| `src/renderer/shared/onboarding-lib.js` | `src/renderer/onboarding/` | `placeBubble` (above-else-below flip, side fallback for tall targets, viewport clamp, tail offset) and the `TOUR_STEPS` registry shape — the [first-time onboarding](onboarding.md). The `onboarding/` modules are the DOM/localStorage shell (spotlight overlay, coach-mark card, cheat-sheet dialog). |
| `src/i18n/index.js` | — | `t()` lookup + English fallback, `setLocale` fallback for unknown codes, `pickLocale` (system-language → locale match on the primary subtag, preference order, English fallback — the first-run auto-detect), and **locale key parity** (every locale has exactly the base `en` key set — catches a string added to `en` but forgotten elsewhere, or an orphan/typo'd key). |

### Static guard: viewer lazy loading

`test/lazy-viewers.test.mjs` has no module-under-test: it walks the renderer's *static* import graph from `src/renderer/index.js` (regex over top-level `import`/`export ... from`; dynamic `import()` deliberately excluded) and asserts (1) none of the lazily loaded viewers (`viewer/asset|sheet|db/index.js`, `viewer/web.js`) nor the heavy packages (`three`, `xlsx`, `paper`, `pdfjs-dist`, `pdf-lib`) are reachable at startup, and (2) each lazy viewer's own eager graph also stays free of those packages (the second layer of laziness — see [center coordinator](architecture.md#renderer)). A stray top-level import of a viewer module anywhere on the startup path fails this test.

### Renderer/i18n helpers are ES modules

The renderer and `src/i18n/` are loaded by the browser as `<script type="module">`, so they use `import`/`export`. Node treats `.js` as CommonJS by default and would choke on that syntax. A folder-scoped `package.json` (`{"type":"module"}`) marks the testable folders — `src/i18n/` and `src/renderer/shared/` — as ESM so Node's test runner and ESLint can load those pure helpers the way the browser does. The browser ignores the file; the Electron main process never imports renderer/i18n code, so runtime is unaffected. Accordingly tests that import an ESM helper are `.mjs` files (`test/i18n.test.mjs`, `test/git-status.test.mjs`); the tests for the CommonJS main-process helpers are plain `.js` (they `require` the helpers).

## Adding a test

Drop a `*.test.js` (CommonJS, `require` the helper) or `*.test.mjs` (ESM, `import` it) file in `test/`. Use `node:test` + `node:assert/strict`:

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
const { thing } = require('../src/main/your-lib');

test('describes the behaviour', () => {
  assert.equal(thing(input), expected);
});
```

If the logic you want to test currently sits inside an `ipcMain.handle(...)` or touches `fs`/`execFile`/the DOM, **first extract the pure part** into a sibling helper (as in the table above), wire the shell to call it, then test the helper.

## Lint

`eslint.config.js` is a flat config with a per-area environment, because the three layers run in different runtimes:

- `src/main/**`, `src/preload/**` → CommonJS, Node globals.
- `src/renderer/**` → ES modules, browser globals (+ the `hljs` CDN global from `index.html`).
- `src/i18n/**` → ES modules, browser globals.
- `test/**` → Node; `*.mjs` is ESM, `*.js` is CommonJS.

Rules are intentionally light — the goal is to catch the failures that otherwise only surface at runtime in the GUI (undefined variables, typos, unused bindings), not to enforce style. `_`-prefixed throwaways (e.g. an `ipcMain` handler's unused `_e`) and empty `catch {}` blocks are allowed.
