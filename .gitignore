node_modules/
dist/
# Bundled Ollama engine binaries, fetched at build time by scripts/fetch-ollama.mjs
# and shipped via electron-builder extraResources. Too large to commit.
vendor/
*.log
# Icon variants derived from assets/icon.png by scripts/gen-icons.js. The
# desktop dev icon is never shipped in a packaged build; mobile/assets holds the
# copies expo prebuild reads.
assets/icon-dev.png
mobile/assets/
crashlogs/
.DS_Store
package-lock.json
# Throwaway workspace the "Start IDE" launch config opens for testing the IDE
# with the IDE. Has its own git repo so the test instance's git pane is isolated.
.test-workspace/
