# Architecture

Standard Electron main/preload/renderer split. The renderer is pure UI with **no** Node or OS access — everything that touches the OS lives in the main process and is reached over IPC declared in `preload/`.

```
renderer/  ──IPC(preload/)──►  main/  ──►  node-pty / git / http hook server
  (UI)                          (OS)
```

## Map — start here

This file is long (~40k tokens). **Don't read it top to bottom** — find the one section you need and read just that (open with an `offset`, or grep a heading). Two ways in:

- **By source file** — the [Files](#files) section at the end maps every `src/` module to the section(s) that explain it. Start there when you're about to edit a specific file.
- **By feature** — the sections below, grouped by where they sit in the UI:
  - *Sessions & their tracked work* — [The main process](#the-main-process--three-subsystems) · [Session persistence](#session-persistence) · [Per-session commit](#per-session-commit) · [Per-session revert](#per-session-revert) · [Per-session diff](#per-session-diff) · [Tracking filesystem changes](#tracking-filesystem-changes)
  - *Git pane* — [Renderer](#renderer) (the git pane lives here) · [Hand a merge to Claude](#hand-a-merge-to-claude) · [Git stash](#git-stash)
  - *Claude integration* — [Haiku generation](#haiku-generation) · [Usage meter](#usage-meter) · [Per-session token meter](#per-session-token-meter) · [Claude Code setup gate](#claude-code-setup-gate)
  - *Center pane & explorer* — [Welcome screen](#welcome-screen) · [File explorer](#file-explorer) · [Quick Open](#quick-open) · [Command Palette](#command-palette) · [File editor](#file-editor) · [Syntax highlighting](#syntax-highlighting) · [Asset viewer](#asset-viewer) · [Terminal links & inline browser](#terminal-links--inline-browser) · [Resizable panes](#resizable-panes)
  - *Run & consoles* — [Run toolbar](#run-toolbar) · [Run a file](#run-a-file) · [Git-pane consoles](#git-pane-consoles)
  - *Cross-cutting* — [Error handling & crash logs](#error-handling--crash-logs) · [preload/](#preload) · [Files](#files)

Other areas live in their own docs: settings/theme/i18n → [settings.md](settings.md); status dots → [status-detection.md](status-detection.md); Windows/PTY gotchas → [platform-notes.md](platform-notes.md); tests/lint/build → [testing.md](testing.md).

## The main process — three subsystems

1. **PTY manager.** One `pty.spawn(claude, ...)` per session, keyed by a generated UUID. The UUID is passed as `--session-id` so it round-trips inside hook payloads — that is how a hook event is matched back to its sidebar row. Sessions live in the `sessions` Map; `pty-input` / `pty-resize` / `kill-session` operate on it by id. Each record stores the **`repo`** it was created in (`getRepoPath()` at `new-session`), so a session is bound to its project: it spawns/resumes with that folder as `cwd` regardless of which folder is currently open, the snapshot persists it, and the renderer shows only the open folder's sessions. Each record also stores the session's **agent model choice** (`model`/`subagentModel`), which `sessionEnv()` turns into the CLI's `ANTHROPIC_MODEL`/`CLAUDE_CODE_SUBAGENT_MODEL` env at spawn (and re-applies on resume), so a session keeps running the model it was created with — see [settings.md](settings.md#agent-models) — switching folders (`setSessionsRepo()`) re-filters the list (on top of the active/archived tab) without tearing down the other projects' live terminals. **Archiving** a session (the sidebar ×) doesn't kill it — `suspend-session` kills only the Claude PTY to free its resources while keeping the session record (and all its tracked-file state) with `pty: null` and `suspended: true`; the `onExit` handler sees `suspended` and skips the usual teardown. **Restoring** it (`resume-session`) respawns with `claude --resume <id>`, so the conversation continues under the same id and `recordSessionActivity()` keeps accumulating `edits`/`fileOps` against the same record — any uncommitted work tracked before archiving survives and stays committable. The renderer disposes the xterm terminal on archive and rebuilds it on restore (`attachTerminal()`), keeping its `files` list intact throughout. Each session record also accumulates **what the agent did**: `firstPrompt` (its first `UserPromptSubmit`); `edits` — a `Map<absPath, op[]>` recording every `PostToolUse` of a file tool (`Write`/`Edit`/`MultiEdit`/`NotebookEdit`) as a replayable op (`editOp()`); and `fileOps` — a `Map<absPath, 'add'|'delete'>` of **path-level changes a text op can't express** (a binary file a `Bash`/MCP tool created, or a file it renamed/moved/deleted), filled by diffing the git working tree across a tool call (see [Tracking filesystem changes](#tracking-filesystem-changes)). `recordSessionActivity()` fills all of these from the hook stream and pushes a `session-meta` event to the renderer (the file list is `trackedFiles()` = the union of `edits` and `fileOps` keys); `commit-session` replays the ops and folds the path-level changes to commit just that session's work (see [Per-session commit](#per-session-commit)). On the first prompt it also fires `generateSessionName()` — a Haiku call via the shared `runHaiku()` helper (prompt capped at 2000 chars) asking for a 2-4 word title, pushed to the renderer as `session-name`. See [Haiku generation](#haiku-generation) for how `runHaiku()` reaches the model.
2. **Hook → status server.** A Node `http` server on a random `127.0.0.1` port. See [status-detection.md](status-detection.md).
3. **Git.** Plain `git` porcelain via `execFile` in `repoPath` (no git library). Every call goes through the shared `git()` helper, which prepends `-c core.quotePath=false` (so non-ASCII paths come back verbatim and round-trip to `add`/`diff` instead of being C-quoted), sets `GIT_TERMINAL_PROMPT=0` (a remote that wants credentials with no helper fails fast instead of hanging on an unanswerable prompt), and applies a 120s `timeout` backstop so a stuck network op can't wedge the UI. `git status --porcelain=v1` is parsed into staged/unstaged lists **and a separate `conflicts` list** — unmerged entries (`DD/AU/UD/UA/DU/AA/UU`, both columns set) are pulled out so they aren't double-listed with wrong-direction stage/unstage actions; the renderer shows them in a dedicated Conflicts section (click to view, ✓ to `git add` once resolved). stage = `git add`, unstage = `git reset HEAD` (falls back to `git rm --cached` for a repo with no commits); also `git commit -m` (called with an empty message, it stages-all-if-needed then asks Haiku to author the message from the staged diff — `generateCommitMessage()`, the shared `runHaiku()` helper in `claude.js` (see [Haiku generation](#haiku-generation)), capped at 12000 chars in / 1000 out; the message used is returned to the renderer), `git-undo` (`git reset --soft HEAD~1` — undo the last commit, keep its changes staged), `git push` (a branch with no upstream fails with "has no upstream branch", so the handler retries once with `push -u origin HEAD` — the common first-push case), `git-fetch`, and `git-pull` (`pull --ff-only`, so a pull that would need a merge fails cleanly instead of opening an editor with no tty or leaving conflicts — the user can then hand the merge to a Claude session, see [Hand a merge to Claude](#hand-a-merge-to-claude)). Both `git-pull` and `git-push` tag their result with **`needsMerge`** — true when the failure is a *divergence the remote moved past* (the pure `pullNeedsMerge`/`pushNeedsMerge` in `git-parse.js` match git's ff-only refusal / push-rejection wording, and *only* those, so auth/network/no-upstream failures aren't mistaken for a merge), which the renderer turns into the Claude offer. `commit-session` commits **only the hunks a single session edited** — see [Per-session commit](#per-session-commit). `git-diff` returns a file's unified diff (`--cached` for staged, `--no-index /dev/null <file>` for untracked) for the center-pane diff viewer. `git-revert` discards a file's changes — `git clean` for untracked, `git restore --staged --worktree` otherwise. The **History tab** is backed by `git-log` (last 100 commits, unit-separator-delimited `hash/short/subject/author/relDate` fields), `git-commit-diff` (`git show --format=<hash>` — the bare patch for the center diff viewer), `git-revert-commit` (`git revert --no-edit <hash>` — a non-destructive revert commit, the right tool for **pushed** history), and `git-undo-commit` (drops an **unpushed** commit by rewriting history: a soft reset for the HEAD commit so its changes stay staged, else `git rebase --onto <hash>^ <hash>`, with a `rebase --abort` fallback if the replay conflicts). `open-folder` runs `git rev-parse --show-toplevel` (`repoRoot()`) so porcelain paths and add/reset line up regardless of which subfolder the user picks. The chosen folder is written to `last-folder.txt` in the shared data dir (`instance.js`'s `sharedDataDir`, not the per-instance profile — so it persists across runs and is shared by all instances) and reloaded on startup (`loadLastFolder()`), so the app reopens the last repo automatically; the renderer picks it up via its initial `refreshGit()`. A **`--folder <path>` CLI flag** (alias `--dir`, both `--folder X` and `--folder=X` forms; parsed by the Electron-free `cli-args.js`'s `parseFolderArg`) overrides this for a single launch: when present and the path exists it becomes the initial `repoPath` directly — it is **not** written back to `last-folder.txt`, so the override is ephemeral and doesn't disturb the folder other instances reopen. This is what lets the **Start IDE** launch config open a throwaway, gitignored `.test-workspace/` (its own git repo, so the test instance's git pane is isolated) — so developing the IDE *with* the IDE doesn't have the test instance fight over the real repo. The flag rides through `npm start` via `runtimeArgs: ["start", "--", "--folder", …]`. **Branches**: `gitStatus` also returns the checked-out branch name (`currentBranch()` = `git rev-parse --abbrev-ref HEAD`, `'HEAD'` when detached) for the header indicator; `git-branches` lists local branches (`git for-each-ref --sort=-committerdate refs/heads`, most-recent first) plus the current one for the selector popover, `git-checkout` switches to a branch (`git checkout <branch>`, failing cleanly when the worktree would be clobbered), and `git-create-branch` creates and switches to a new branch off the current HEAD (`git checkout -b <branch>`, surfacing git's rejection of invalid ref names).

## Haiku generation

`runHaiku()` (in `claude.js`) backs every short generation (commit messages, session titles). It has two paths:

1. **Fast path — direct Messages API.** A one-shot `claude -p` cold-starts the whole CLI (Node + plugins + hooks + MCP) and sends its full agentic system prompt + tool definitions on every call — ~5s for a one-line commit message. Instead `runHaiku()` calls `https://api.anthropic.com/v1/messages` directly with the OAuth token Claude Code already stores (`claudeAiOauth.accessToken`), a one-line system prompt, and no tools: **~1s, no process boot, no new dependency or API key.** `readOauthToken()` reads that token from wherever the CLI keeps it for the platform — the `~/.claude/.credentials.json` file on Linux/Windows, or the **macOS Keychain** (generic password, service `Claude Code-credentials`) on darwin, where no file exists. It tries the file first, then falls back to the Keychain via `security find-generic-password`; both yield the same JSON blob. (Miss this and the whole fast path — and the usage meter below — silently fails on macOS, since every call falls back to the slow CLI and `readUsage()` returns null.) Subscription (OAuth) tokens are only accepted when the **first system block is the Claude Code identity string** (`"You are Claude Code, …"`) — the same one the CLI sends; a second system block carries the "output only the requested text" rule. Sent with headers `authorization: Bearer <token>`, `anthropic-version: 2023-06-01`, `anthropic-beta: oauth-2025-04-20`.
2. **Fallback — the CLI.** The OAuth token is short-lived; `readOauthToken()` returns null when it's missing or within 60s of `expiresAt`, and any non-200/transport error also yields null. In those cases `runHaiku()` falls back to the original one-shot `claude -p --model haiku` (prompt over stdin). That path is slow but always works **and refreshes the stored token on disk**, so the fast path heals itself on the next call. Both callers already treat a null result as "Haiku unavailable" and fall back further (the session title for a per-session commit, an error for the main git pane).

The model id is pinned (`claude-haiku-4-5-20251001`) because the raw API needs a full id, not the `haiku` alias; if it ever 404s the call returns null and the CLI fallback (which does accept the alias) takes over.

## Usage meter

The top toolbar shows the user's remaining **Claude subscription usage** against the two rolling windows — the **5-hour** session limit and the **weekly** limit — as two **vertically stacked** sliders (5-hour on top, weekly below) with a "resets in …" countdown, just left of the browser/settings buttons. The run strip (`#toolbar-runs`) flex-grows and scrolls internally, so the meter (and the browser/settings buttons) stay pinned to the right edge no matter how many run buttons there are. The meter is **hidden entirely when there's no usable OAuth token** — a machine without Claude Code installed, or one not yet signed in — so it never shows during first-run setup.

There is **no CLI command or dedicated endpoint** for this — `/usage` is interactive-only, and `count_tokens` returns no rate-limit headers (verified live). The data rides on the **unified rate-limit response headers of a real `/v1/messages` call**: `anthropic-ratelimit-unified-5h-utilization`/`-5h-reset`, the `7d` pair, and `-representative-claim` (`five_hour`/`seven_day`, the current bottleneck). Utilization is a `0..1` fraction; reset is **epoch seconds**. So `readUsage()` (in `claude.js`) makes the **smallest possible** call — `max_tokens: 1` (≈ one output token), the same OAuth token + Claude Code identity system block as `runHaiku()` — and reads the headers off the response; it returns null when there's no usable OAuth token or the call fails. The renderer polls `get-usage` (sessions.js) **once a minute** (the headers' own granularity) and hides the meter on null (an API-key user, or a transient failure), so the per-minute cost is one token and no perceptible latency.

The header→view-model parsing is the Electron-free, unit-tested `usage-parse.js` (`test/usage-parse.test.js`): `parseUsageHeaders` (both windows, utilization clamped to `0..1`, claim mapped to the `5h`/`7d` key, null when the headers are absent), `formatResetShort` (ms-until-reset → compact `24m`/`13h`/`2d`/`now`), and `usageView` (the assembled `{windows:[{key, utilization, resetIn, resetAt, representative}]}`). The renderer (`usage-meter.js`) renders each window's fill width from utilization and colours it by level — green (<50%) / `warn` yellow (≥50%) / `crit` red (≥80%) off the `--green`/`--yellow`/`--red` palette vars — and re-translates labels via `data-i18n` (`usage.*` keys); both windows' labels are styled identically. The two rows share a 3-column grid (`label · bar · reset`, each `.um-win` is `display:contents`) so the bars line up regardless of label width or locale. reset countdowns refresh on the next poll.

## Per-session token meter

Distinct from the subscription [Usage meter](#usage-meter) above: each session's terminal shows its **own** cumulative token count and dollar cost on a status line at the very bottom, driven by Claude Code's native **statusLine** feature — no app UI, no terminal scraping. It rides on the same `claude --settings <json>` flag that already injects the hooks (`hooksSettings(port, statusLineCommand)` in `hook-events.js`), so the user's global settings are never touched: when a command is supplied, a `statusLine: { type: 'command', command }` entry is added alongside the `hooks`.

Claude pipes the session's status JSON to that command's stdin on each refresh and renders whatever it prints. The command is `node <script>`, where the script (`statusline-script.js`) reads stdin and emits `model · $cost · tokens` via the pure, unit-tested `statusline-lib.js` (`formatStatusLine`). The **metrics are right-aligned**: the model sits on the left and the `$cost · tokens` block is pushed to the terminal's right edge using the width Claude exposes in the `$COLUMNS` env var (stdout is piped, so `process.stdout.columns` is unset). The padding is placed *between* the two halves rather than leading, so the alignment survives even if Claude trims leading/trailing whitespace; when `$COLUMNS` is absent or too narrow it falls back to a plain ` · `-joined inline line. The **cost is placed *before* the token count**: in a right-aligned line Claude clips the right edge, so the (less critical) token count absorbs any truncation while the dollar — to its left — stays fully visible. A 1-column right `margin` plus `padding: 0` on the statusLine config further keep the metrics off the edge. The cost renders in the same default colour as the rest of the line (no ANSI emphasis). Claude Code's `$COLUMNS` and `padding` support are what make the alignment work (`statusline-lib.js`'s `formatStatusLine` takes `{ width, margin }`).

**Toggle.** A **Settings → General → Token cost meter** switch (default on) gates the whole thing. Because the statusLine is injected in main at spawn, the renderer persists the flag in `localStorage` *and* pushes it to main over IPC (`set-statusline-enabled` → `statusline.setEnabled`); `statusLineCommand()` returns null when off, so the next session spawns with no statusLine while live sessions keep theirs. See [settings.md](settings.md#general-preferences). **Tokens are summed from the transcript, not stdin**: the stdin `context_window` counts are *current context size*, not the session total, so `sumTranscriptTokens()` reads `transcript_path` and totals all four token classes (`input`/`output`/`cache_creation`/`cache_read`) across every assistant call — the same basis the cost is computed on. **Cost is taken verbatim** from stdin's `cost.total_cost_usd` (already cumulative), so there's no pricing table to maintain. A leading BOM is stripped before parsing and any bad/empty stdin degrades to `0 tokens · $0.00`.

**Why a script staged on disk.** Claude runs the statusLine command as an external process, so the script must live on real disk — an external `node` can't read inside the packaged asar archive. `statusline.js` copies both files (`statusline-script.js` + its `statusline-lib.js` sibling) out of the app bundle into `sharedDataDir/statusline/` once per launch (Electron's fs reads the asar source transparently) and returns the `node "<path>"` command; it returns null if staging fails, in which case no statusLine is injected. **`node` is assumed on PATH** (a developer's machine running a Node CLI); if it's absent Claude simply renders no status line and the session is otherwise unaffected.

**On a subscription the dollar figure is notional** — `total_cost_usd` is computed at API rates, but a Pro/Max plan isn't billed per token (the truthful "how much of my plan" number is the [Usage meter](#usage-meter)); this line answers "how heavy is this session".

## Claude Code setup gate

The whole app is a front-end for the `claude` CLI, so a user who hasn't installed Claude Code is guided through it before any session can spawn. The check runs on **every** app launch (nothing is persisted to skip it after the first run), so an install that later breaks or is removed is caught again. `claudeAvailable()` (in `claude.js`) is the detector: it clears the cached `resolveClaude()` path (so a fresh install is picked up on re-check) and runs `claude --version` with a 10s timeout — the truest "actually usable" test, since a stale PATH entry that no longer launches still counts as missing; any failure (not on PATH, non-zero exit, timeout) resolves to `{ installed: false }`. The `check-claude` IPC handler (`sessions.js`) returns that result plus `installGuide()` from the Electron-free, unit-tested `claude-install.js` (`test/claude-install.test.js`).

`installGuide(platform)` gives, as one object: `installArgs`, `authArgs`, `installOk`, and `installFail`. The crucial design choice is **how the commands run**: not by *typing* them into an interactive shell prompt, but by spawning the shell with the command as **argv** (`zsh -ilc '<cmd>'`). Typing into a freshly-spawned interactive shell races its line editor — zsh's ZLE on macOS swallowed the command, so the install terminal looked alive but did nothing. Passing the command as a shell argument runs it directly and reliably. `installArgs`/`authArgs` are therefore arrays handed straight to `pty.spawn` (via the new `args` field on `term-create`); the shell path itself stays main's choice (`availableShells()[0]` — `$SHELL` on POSIX, `powershell.exe` on Windows).

The native installer needs nothing pre-installed on any platform (Windows ships PowerShell's `irm` + .NET, macOS/Linux ship curl + bash), so there is a single install path and no npm fallback. POSIX uses `-il` (login + interactive) so the shell sources the user's profile/rc and inherits their **real** PATH/env — a GUI-launched macOS app otherwise gets a stripped-down launchd environment (not what the user has in Terminal.app), which is why an install could appear to run yet not take. Re-sourcing the rc also puts the freshly-installed `claude` on PATH for the sign-in step (`authArgs` = `['-ilc', 'claude']`). Windows PowerShell has no login concept and inherits the full env, so `-NoExit -Command` is enough, with the auth command re-reading PATH from the registry (the installer wrote it there) before launching `claude`. Only the non-translatable shell commands live in the helper; all surrounding prose is i18n (`setup.*` keys).

**`installOk` / `installFail` — knowing the outcome.** The install runs in an embedded terminal whose shell stays alive afterward, so there's no exit code to read. The install command echoes `installOk` on success and `installFail` otherwise, keyed on the installer's exit status (`… && echo OK || echo FAIL` on POSIX, `…; if ($?) { echo OK } else { echo FAIL }` on Windows). The renderer watches the terminal output for whichever appears: **OK** enables Next, **FAIL** keeps it disabled and shows a retry message. (An earlier single "completion" marker chained with `;` printed even on failure, so a failed install was reported as success — the bug this replaced.) Because the command is spawned as argv rather than typed, the shell never echoes the command line, so no quoting trick is needed to keep the marker out of a command echo.

The renderer side is `claude-setup.js` — a **three-step wizard** (`#claude-setup-dialog`) with a numbered stepper: **Install → Sign in → Done**. `initClaudeSetup()` runs on every launch (after `initSettings` so it's translated, after `initConsoles` so the terminal subsystem is up) and opens the wizard when Claude Code is missing. The flow is deliberately easy — **Next → Next → authenticate in the terminal → Finish**:

- **Intro** (step 1) just states Claude Code isn't installed and offers **Next**; the detected platform is shown as a hint.
- **Install** (step 2): entering it spawns an xterm terminal *embedded in the dialog* (the same `shared/terminal.js` helpers the sessions/consoles use) with `guide.installArgs` as the shell argv via `term-create`. **Next stays disabled** until the renderer sees `guide.installOk` in the terminal output; `guide.installFail` keeps it disabled and shows a retry message. The note tells the user to wait and click Next when it finishes.
- **Sign in** (step 3): the install terminal is **torn down** (killing that shell) and a fresh terminal is spawned with `guide.authArgs` — i.e. `claude` running directly — so the login flow (browser or paste-code) runs inline. **Finish** verifies with a real `claudeAvailable()` probe before closing; on failure it surfaces the "you may need to restart the app" note (a just-installed CLI often isn't on the already-running process's PATH). The setup-docs link opens in the system browser via `openExternal`, never navigating the app window.

Auth completion itself isn't programmatically tracked (there's no reliable cross-OS signal short of a real API call) — the user drives it in the terminal and clicks Finish, which is gated on the install probe as the source of truth. The embedded terminal's `term-data`/`term-exit` listeners filter by its own `termId`, so they coexist with the git-pane consoles' listeners on the same broadcast channels. Going **Back**, or closing the dialog (×, Esc, or a successful Finish), tears the embedded terminal down (kills the PTY, disposes the term) so no background install/auth shell is left running. `newSession()` also routes through `ensureClaude()`, which re-probes and re-opens the wizard (returning `false` so the caller bails) whenever Claude Code is still absent — so every entry point to opening a session is gated, not just startup.

## Per-session commit

Each session is tracked independently so its work can be committed on its own, **even when two sessions edit the same file** — each commit then contains only the lines that session changed.

**Tracking (record).** Every hook payload carries the `--session-id` we spawned with, so `recordSessionActivity()` can attribute it. Per session it keeps:

- `firstPrompt` — the first `UserPromptSubmit` (set once). Seeds the Haiku-generated session **title**, and is the last-resort fallback for a per-session **commit message** (see below).
- `edits` — a `Map<absPath, op[]>`. Every `PostToolUse` of a file tool becomes a *replayable op* via `editOp()`: `Write` → `{t:'write', content}`, `Edit` → `{t:'edit', old, new, all}`, `MultiEdit` → `{t:'multi', edits}`, `NotebookEdit` → `{t:'opaque'}` (can't be replayed as text). A `.gitignore`d file is **never** recorded here — `recordSessionActivity()` runs `isIgnored()` (`git check-ignore`) on the edited path first and skips it, so a session editing an ignored file (a log, `.env`, a build artifact) never tracks or commits it. This mirrors the filesystem-diff path, which already excludes ignored files because they don't appear in `git status`.
- `fileOps` — a `Map<absPath, 'add'|'delete'>` of path-level changes no text op can express (a binary a `Bash`/MCP tool produced, or a rename/move/delete), filled by diffing the working tree across each tool call. See [Tracking filesystem changes](#tracking-filesystem-changes).

**Commit (replay).** `commit-session(id)` rebuilds, for each touched file, *only this session's version*:

1. `replayEdits(base, ops)` replays the session's ops onto the file's committed (`HEAD`) contents — `base` comes from `git show HEAD:<path>` (empty for a new file). String replacement, in order. It returns `{content, clean}`; `clean` is false if an `old_string` is no longer present (the other session moved/overwrote it) or the op was opaque.
2. If `clean`, the synthesized `content` is committed. If not, that file falls back to its **whole current working-tree contents** (best effort — overlapping same-line edits are inherently ambiguous). This is `commitContent(base, ops, working)` in `edit-ops.js` (pure, unit-tested): replay-or-fall-back, returning the blob to commit. **Empty patches are dropped here**: when the resolved content is byte-identical to the `HEAD` blob (the session edited a file then undid the change, or rewrote identical text) it returns `null` and the file is committed as *nothing* — so the commit and the `Commit N files` count never include diff-less phantom changes. `fileOps` get the same empty-patch filter: an `'add'` whose current bytes already match `HEAD` (compared by `hash-object` blob sha, binary-safe) and a `'delete'` of a path not in `HEAD` are both dropped. Dropped phantom paths are **forgotten from `edits`/`fileOps` unconditionally** (even when no real commit follows) and a `session-meta` is re-pushed, so a button that read `Commit 13 files` corrects to the real count (or `Nothing to commit`) instead of "committing" files git then silently ignores.
   Then the session's `fileOps` are folded in as extra commit entries (skipping any path already covered by an edit): an `'add'` reads the file's **current bytes as a `Buffer`** (so binary stays intact) and an `'delete'` becomes a removal entry. This is what gets binary files, renames/moves, and deletions into the per-session commit alongside the text hunks.

   **Commit message.** A session's first prompt is conversational ("make the git panel nicer"), not a commit message, so the message is authored from the session's **own diff** instead — the same patch the Diff dialog renders. `sessionCommitMessage(s, id, patch)` feeds that patch to Haiku via `runHaiku()` (`commitMessagePrompt`/`cleanCommitMessage` in the pure, unit-tested `commit-msg.js`, identical phrasing to the main git pane's `generateCommitMessage` in `git.js`). The prompt encodes the two widely-used standards — the [Conventional Commits 1.0.0](https://www.conventionalcommits.org/en/v1.0.0/) structure (`<type>(scope): <subject>`, body, `BREAKING CHANGE:` footer) and [Chris Beams' seven rules](https://cbea.ms/git-commit/) (imperative ≤50/72-char subject, blank line, a body wrapped at 72 explaining what & why) — so the log reads professionally instead of as one-line stubs. `cleanCommitMessage` strips a stray ``` fence / wrapping quotes and caps the result at 4000 chars so a full subject+body+footer survives. The call is bounded by a 30s timeout (`Promise.race`); on timeout, an empty diff, or any failure it falls back to `fallbackCommitMessage` — the session **title** (`s.name`), then the first prompt's opening line, then `session <id8>`. Because the call adds latency, the work is **snapshotted at click**: the committed paths are removed from `edits`/`fileOps` and a `session-meta` is pushed *before* the await, so a session that keeps running during generation accumulates its new edits as a separate batch that this commit never folds in (the frozen `entries` already hold the click-time blob content). If the commit then fails, the snapshot is restored (newer edits kept ahead) so nothing is silently dropped. The renderer's commit button shows a `Writing commit message…` spinner and blocks re-clicks until it resolves (see [Renderer](#renderer)).
3. `commitBlobs(entries, msg)` commits those entries **without touching the working tree or the real index's other entries**: it seeds a throwaway index (`GIT_INDEX_FILE` in a tmp file) from `HEAD`, `hash-object -w`s each blob entry (`content` may be a string or a `Buffer`, so binary is byte-exact) and `update-index --add`s it — or `update-index --force-remove`s a `delete` entry — into that throwaway index, then `write-tree` → `commit-tree -p HEAD` → `update-ref HEAD`. Finally it syncs the **real** index for *just those paths* (new blobs via `--cacheinfo`, removals via `--force-remove`), so the committed changes read as clean and only the *other* session's edits remain as unstaged changes.

Because step 3 commits a synthesized blob rather than the shared working file, the other session's edits to the same file are never swept in — they stay in the working tree for that session to commit later (replaying onto the now-updated `HEAD`).

On success `commit-session` **forgets the committed paths** from that session's `edits` *and* `fileOps` maps and re-pushes `session-meta` with the shrunk file list (`trackedFiles()` = union of the two). The renderer's per-session commit button shows `Commit N files` while changes remain, and a disabled **`Nothing to commit`** once there are none. Crucially **`N` is the committable count, not the raw tracked count**: `trackedFiles()` includes *empty-patch* files — ones whose net change vs `HEAD` is now nothing (already committed via the main git pane, or edited-then-reverted) — which `commit-session` drops, so counting them would make the button read `Commit 20 files` while the commit only produces 5. The button therefore reads `N` from the session's **diff stat** (`s.diffStat.files`, from `session-diff-stat` → the same `sessionEntries` filter the commit and the Diff badge use), falling back to `trackedFiles().length` only until the first stat arrives. `refreshDiffStat` runs off every `session-meta` and on select/restore and re-renders the button when the fresh count lands, so the label, the Diff badge, and the actual commit always agree. So the button doubles as the commit-state indicator — after a commit it goes disabled until the session edits again, and can be re-used many times across a session. There is no separate persistent "Committed" status label; `#session-commit-msg` only carries the transient *revert* result and commit/revert *errors* for the active session.

**Known ceilings.** Two sessions editing the *exact same lines* → the second replays unclean and falls back to a whole-file commit. `NotebookEdit` is always whole-file. A version of a session's file the user manually staged is overwritten in the index by the synthesized blob. This relies on Claude Code's `PostToolUse` payload exposing `tool_input.old_string`/`new_string`/`content`. For `fileOps`, the executable bit isn't preserved (everything commits as mode `100644`), `.gitignore`d files are never tracked (the text-edit path filters them via `git check-ignore`, the filesystem-diff path because they don't appear in `git status` — the intended behaviour in both), and attribution is per-burst against a global `git status` — two *different* sessions whose `Bash` calls overlap in time can still mis-attribute each other's filesystem changes (the snapshot can't tell which session wrote a path). Within a single session, parallel tool calls are handled correctly via the in-flight ref-count (see [Tracking filesystem changes](#tracking-filesystem-changes)).

## Per-session revert

The symmetric operation: `revert-session(id)` undoes **only what this session did in the working tree**, leaving another session's edits to the same file intact. It does *not* snapshot — it de-applies the session's own ops.

For each touched file, `inverseEdits(working, ops)` is the inverse of `replayEdits`: it walks the session's ops newest-first and backs each substitution out of the *current* working contents (`Edit` `old→new` is reversed to `new→old`). Because each reversal only rewrites this session's own `new` strings, another session's edits (different text regions) are untouched. The reverted contents are written straight to the working file.

`clean=false` when an op can't be inverted — a full `Write` or opaque op (no stored pre-image), a pure deletion (can't relocate the removed text), or a `new` string that's since been overwritten. Then a hard reset to `HEAD` is only done when **no other live session also edited that file** (`git show HEAD:<path>`, or delete if the file was new this session); if another session shares the file it's **skipped** and reported, since clobbering its work is worse than leaving ours. Successfully reverted files are dropped from the session's `edits` map (skips stay tracked so the user can retry after resolving); a `session-meta` event refreshes the bar's file count.

The session's `fileOps` are undone the same way: a path tracked in `HEAD` is restored with `git checkout HEAD -- <path>` (binary-safe — git writes the committed bytes directly), an `'add'` with no `HEAD` version was new this session so it's `unlink`ed, and a path another live session also touched (`edits` **or** `fileOps`) is **skipped**. Reverted `fileOps` are forgotten too; the `trackedFiles()` count covers both maps.

## Per-session diff

The session bar's **Diff** button shows what a single session changed, scoped the same way commit/revert are — only that session's own hunks vs `HEAD`, never another session's edits to the same files. `session-commit.js` factors the blob-building out of the commit handler into `sessionEntries(s, repoPath)` (the replay-or-fall-back per file, plus the binary/rename/delete `fileOps`, identical to what [Per-session commit](#per-session-commit) commits), so commit and diff agree exactly. `sessionDiff(s, repoPath, withPatch)` then seeds a **throwaway index** from `HEAD` (the same `GIT_INDEX_FILE` trick as `commitBlobs`, so the real index and working tree are untouched), `update-index`es each entry's blob into it, and runs `git diff --cached` against `HEAD`: `--numstat` gives the badge counts (summed by the pure, unit-tested `sumNumstat` in `git-parse.js` — a binary file counts as a changed file with 0 lines), and the full patch (only when `withPatch`) feeds the dialog. An empty entry set is "no change", so the button disables.

Two IPC handlers: `session-diff-stat(id)` returns just `{additions, deletions, files}` for the button badge; `session-diff(id)` adds the `patch` for the dialog. The renderer refreshes the badge off **every `session-meta`** (debounced per id) — which main pushes for background sessions too, so a session working out of view keeps its `+added/-removed` current — plus once per restored session on startup (no push fires for an idle restored session) and on select. A session's stat is computed **against `HEAD`**, so it also goes stale whenever `HEAD` moves underneath it without that session editing anything — a commit from the main git pane, a commit from *another* session, or a file changed on disk — leaving a button stuck on `Commit 2 files` that reads `Nothing to commit` the moment the user opens the diff (which forces a refresh). To close that gap, `refreshGit()` (the single choke point hit after any commit, on the 3s poll, and on window focus) re-validates **every** session's stat via `refreshAllDiffStats()` (in `sessions.js`) whenever the working-tree state actually changed — gated on a compact `gitStateSignature(r)` fingerprint (branch + ahead/behind + every staged/unstaged/conflicted path-and-status) so the poll doesn't fan out a diff-per-session when nothing moved. Each per-session refresh is the same debounced `refreshDiffStat`, so this and a concurrent `session-meta` coalesce. Clicking **Diff** opens `#session-diff-overlay`, a panel **over the live terminal** (the terminal is never torn down, so the session is one dismiss away); the overlay **darkens + blurs the backdrop** (`rgba(0,0,0,.45)` + `backdrop-filter: blur`) like the approval/confirm dialogs. It closes on its ×, on any `mousedown` outside the panel (a capture-phase document listener, so clicking the git pane / Commit button dismisses it and the user can commit straight away), on Escape, and on switching sessions.

The dialog has two view modes, toggled by a segmented **Unified / Split** control in its head (the choice persists across opens; the held `patch` re-renders without re-fetching): **Unified** is the single-column view via `renderDiffInto()`, and **Split** is a two-column old-|-new side-by-side via `renderDiffSplitInto()` — within a hunk it pairs consecutive deletions and additions row-for-row (del left, add right; an unpaired one leaves the opposite side a blank filler), context lines show on both sides, and file/`@@` headers span full width. Both are factored out of `diff.js` (`fileHeaders` on for the per-file headers a multi-file session patch carries) and render into the dialog's own body rather than the center diff overlay — that overlay hides the terminal, which is exactly what this dialog must not do.

## Hand a merge to Claude

A diverged remote is the one git situation the porcelain buttons deliberately *don't* resolve themselves (a real merge needs judgement and can conflict). Instead the IDE offers to hand it to a fresh Claude session, so a non-expert user never has to drop to a terminal. Three entry points, all funneling through `offerClaudeMerge(title, message, errorText)` in `git-pane.js`:

- **A failed pull** — `git-pull` came back `needsMerge` (the branches diverged, so `--ff-only` refused).
- **A rejected push** — `git-push` came back `needsMerge` (the remote has commits this branch doesn't; the push bounced). Other push failures still show the plain git-error dialog.
- **The Conflicts section's "Let Claude resolve" button** — for conflict markers already sitting in the working tree (`#conflicts-resolve`, in the section header).

`offerClaudeMerge` first `confirmDialog`s (so it's opt-in, never automatic), then calls `newSessionWithPrompt(prompt)` (exported from `sessions.js`). The prompt is **one generic instruction** — pull and push the current branch, resolve any conflicts, run the tests, don't break a feature (`git.mergePrompt`) — with the **exact git error appended** (`git.mergePromptError` + `r.stderr`), so the agent has the real failure text instead of re-deriving it. One prompt covers all three cases; only the dialog's title/message differ. `newSessionWithPrompt` spawns a normal session and queues the text, typing it in once the session emits its first PTY output (Claude's input box isn't ready the instant the PTY spawns), then **submits it** with a separate Enter write a beat later — bundling the `\r` with the text submits before the TUI finishes ingesting the multi-line paste, so the Enter is deferred and sent on its own. All strings are i18n'd (`git.*` keys); the divergence detection itself is the unit-tested `pullNeedsMerge`/`pushNeedsMerge` in `git-parse.js`.

## Git stash

Setting the whole working tree aside (and bringing it back) rounds out the porcelain buttons, for the common "I need to switch context but don't want to commit half-done work" case. It's modelled as a **section in the Changes view** — a fourth signal color (purple `git-stashes-title`) alongside Staged/Changes/Conflicts — that, like the Conflicts section, **renders only when there's something to show** (`stashes.length`). Creation is a single **Stash** button (archive icon) in the Changes header beside stage-all/discard-all: `git stash push --include-untracked` (so new files are saved too). The renderer reads git's "No local changes to save" out of the result to report *Nothing to stash* rather than a misleading success.

Each stash row reuses the commit-row layout (a message line over a muted `ref · relDate` meta line) and three hover actions: **Apply** (`stash apply`, keep it), **Pop** (`stash pop`, apply then remove), and **Drop** (`stash drop`) — Drop being destructive uses the same two-click armed rotate-ccw as the discard buttons (`armHint.dropStash`). Clicking the row opens the stash's patch in the center diff overlay (`git-stash-show -p` → `openStash` → `showStash`, the same multi-file `renderDiff` path commits use). Each op funnels through `runStash()` which surfaces a git error via the shared dialog and then `refreshGit()`s — and `refreshGit()` always calls `refreshStashes()` (an independent `git-stash-list`) so the section, the file lists, and the per-session counts re-render together; it's unconditional because a `drop` changes the stash list without touching the working tree (so the `gitStateSignature` gate would miss it). The selector (`stash@{N}`, from `%gd`) is what targets a specific stash; the message (`%s`) and date (`%cr`) come from `git stash list`, parsed by the Electron-free, unit-tested `parseStashList` in `git-parse.js`. All labels/tooltips are i18n'd (`git.stash*` / `armHint.dropStash`).

## Tracking filesystem changes

The text-edit tools (`Write`/`Edit`/`MultiEdit`/`NotebookEdit`) are the only ones whose `PostToolUse` payload tells us *what* changed in a file. Everything else a session does to the filesystem — a binary a `Bash` command produced (a build artifact, a downloaded asset, an image an MCP tool generated), a `mv`/`git mv` rename, a file moved in or out of the repo, an `rm` — leaves no replayable op. Those are caught instead by **diffing the git working tree around the tool call** (`sessions.js`):

- On **`PreToolUse`** for any file-touching tool — `tracksFs(payload)`: not a text-edit tool and not in the read-only set (`Read`/`Grep`/`Glob`/…) — `recordSessionActivity()` snapshots `git status --porcelain=v1 --untracked-files=all --no-renames` into `s.preStatus` as a `Map<relPath, "XY">`. `--no-renames` makes a rename surface as a separate delete + add, so each path is attributed on its own.
- On **`PostToolUse`** for the same tool, it snapshots again and `applyFsDiff()` compares: a path whose status changed and that **now exists on disk** is recorded `fileOps[abs] = 'add'`; one that's **gone** is recorded `'delete'` — unless it was only ever untracked (never reached `HEAD`, so there's nothing to commit as a deletion), in which case it's simply forgotten. A path already covered by an `edits` op is left to that machinery; a path an edit tracked but that a later tool then moved/removed is dropped from `edits` (its text ops are now void) and recorded as a `fileOp`.

**Bulk-VCS commands are excluded.** A `Bash` command that only *moves git state* — `git pull`/`merge`/`rebase`/`reset`/`stash`/`cherry-pick`/`revert`/`clone`/`switch`/branch-`checkout` — changes the working tree wholesale, but those files are not the session's authored work; attributing them would inflate the per-session commit with dozens of files the agent never wrote (the bogus "Commit 20 files"). This is especially live because the IDE itself spawns sessions to resolve merges (see [Hand a merge to Claude](#hand-a-merge-to-claude)), so an un-skipped `git pull` was the main offender. `tracksFs(payload)` therefore returns false for such a Bash command — classified by the pure, unit-tested `isBulkVcsCommand()` in `fs-track.js` (`test/fs-track.test.js`), which token-scans the command (handling compound `&&` chains, global options like `-C <path>`, and a `--` pathspec separator that marks `git checkout -- file` as a real file restore, not a branch switch). `git mv`/`git rm`/`git add` are **not** excluded — they're genuine path-level edits the tracker must catch.

The snapshot must be taken **before** the tool runs, so the hook server `await`s `recordSessionActivity()` *before* answering the hook — a command hook blocks its tool until the `curl` returns, so awaiting the `PreToolUse` snapshot guarantees the "before" state predates the tool's writes. This adds one `git status` of latency to each file-touching tool (none to read-only tools or non-tool events), which is negligible for an interactive session. Because the diff is scoped to `git status`, `.gitignore`d paths are invisible (correctly never committed).

**Parallel tool calls.** Claude runs tool calls in parallel within a turn, so the `PreToolUse`/`PostToolUse` hooks of several fs tools interleave — a single `s.preStatus` slot would let a second `Pre` clobber the first, and a `Post` arriving with no live snapshot would drop its tool's changes entirely (the "session changed files but the button says nothing to commit" bug). Instead `recordSessionActivity()` **ref-counts the fs tools in flight** (`s.fsInFlight`): it snapshots when the count goes 0→1 and runs `applyFsDiff()` once it returns to 0, diffing every concurrent tool's changes against that one consistent baseline exactly once. The attribution window is therefore *a burst of concurrent tool calls*, not a single call — see the per-commit *Known ceilings* for the cross-session concurrency caveat.

## Session persistence

Sessions survive closing the app: each is stored as **its own file** under `sessions/<id>.json` in the shared data dir (`instance.js`'s `sharedDataDir`, not the disposable per-instance profile), so a single session's change rewrites only that one small file and the whole set is never serialized at once. `serializeSession()` (pure, in `session-persist.js`) snapshots a record to JSON — dropping runtime-only fields (the PTY, the transient `preStatus`) and flattening the `edits`/`fileOps` Maps to arrays; `deserializeSession()` rebuilds it on load with `pty: null` and `suspended: true` (a restored session has no live process until resumed) but its tracked-file state intact, so it's committable immediately.

**Only sessions with something to restore are kept.** `isSessionPersistable(s)` (pure, in `session-persist.js`) gates every write and every load: a session is persistable only once it carries a conversation (`firstPrompt`, set on its first prompt) or tracked work (`edits`/`fileOps`). An **empty husk** — created but never prompted — has no transcript on disk, so resuming it would run `claude --resume <id>` against a conversation that was never created and the CLI prints *"No conversation found with session ID: &lt;id&gt;"*. `new-session` still persists on create, but that write is a no-op until the first prompt; `loadPersistedSessions()` drops (and `removeSessionFile()`s) any non-persistable snapshot so an already-saved husk doesn't resurrect every launch; and the PTY `onExit` deletes the file for a non-persistable session that just exited (the failed-`--resume` case). This is why an unnamed never-used session — even one you archived — no longer reappears with the "No conversation found" error after a restart. Each file also carries a `_seq` — a monotonic creation index — because per-file storage loses the Map's insertion order across a restart; load reads every `*.json`, sorts by `_seq`, and rebuilds the Map oldest-first (and bumps the live counter past the max seen). An existing single-file `sessions.json` store is **migrated** into per-session files on first load (each entry gets a fresh `_seq`), then the legacy file is deleted.

**Every change syncs.** A save is triggered after each mutation: discrete user/state actions — creating a session, archiving (`suspend-session`) and dearchiving (`resume-session`), a `setSessionState()` dot change, a per-session commit (marks `pushed`, and clears the committed edits) or revert (clears the reverted edits), and the Haiku-generated name — call `persistSession(id)` for an **immediate** single-file write; the high-frequency edit stream (`recordSessionActivity()` on every `PostToolUse`) is the one path that **debounces** (`schedulePersist(id)`, ~1s per session) so a burst of edits coalesces into one write. Killing a session (`kill-session`) and eviction delete its file (`removeSessionFile()`); on quit `persistSessions()` writes the whole set synchronously as a backstop. The on-disk set is capped at **100 MB** (`MAX_PERSIST_BYTES`): after each write `evictOverBudget()` drops the oldest *evictable* sessions (`enforceLimit()`, never one with a live PTY) from memory, disk, and the UI (`session-evicted`) until it fits. `serializeSession`/`deserializeSession`/`isSessionPersistable`/`sessionBytes`/`enforceLimit`/`persistedState` are all Electron-free and unit-tested (`test/session-persist.test.js`); the dot-state collapse on persist is covered in [status-detection.md](status-detection.md#persistence-across-restarts).

## Renderer

The renderer is split across `renderer/` modules (`sessions.js`, `git-pane.js`, …); this section covers the session bar + git pane. It keeps a `Map` of sessions, each owning its own xterm.js `Terminal` in a hidden container div. Switching sessions just toggles which container is visible, preserving scrollback. `setState()` sets the status-dot CSS class. Clipboard handling for every terminal (session and git-pane console alike) is the shared `attachClipboard()` in `renderer/shared/terminal.js`: Ctrl+C copies the selection (passing SIGINT through to the PTY only when nothing is selected — key matched case-insensitively so it still fires with Shift held), Ctrl+V / right-click paste, and right-click copies when a selection exists. Copies go through `copyToClipboard()` → main's `clipboard-write` handler (`clipboard.writeText`), because the renderer's async Clipboard API is unreliable under `file://` (not a secure context, so `navigator.clipboard` can be `undefined` and throw synchronously — which is why Ctrl+C silently did nothing) and otherwise needs document focus the canvas can lack right after a selection; a hidden-textarea `execCommand('copy')` remains only as a last resort if the IPC bridge is absent. Text paste likewise reads through main's `clipboard-read` handler. All three clipboard handlers (`clipboard-read`, `clipboard-write`, `paste-image`) retry through the pure `clipboard-lib.js` `withClipboardRetry()`: on Windows the clipboard is a single lock another process can briefly hold, so a bare `readText()`/`writeText()`/`readImage()` intermittently throws "OpenClipboard failed"; without the retry that throw **rejected the IPC call and the renderer's `paste()` swallowed the rejection, silently dropping the paste** (the intermittent "can't paste, time to time" bug). The retry returns a fallback (`''`/`false`/`{ok:false}`) after a few short-delayed attempts instead of rejecting, and `paste()` itself is wrapped in try/catch as a final guard so a stubborn lock degrades to a harmless no-op. Paste checks the clipboard for an **image** before falling back to text: a PTY can't carry image bytes, so main's `paste-image` handler (`clipboard.readImage()`) spills the bitmap to a temp PNG under `os.tmpdir()/claude-ide-pastes` and returns its path, which the renderer pastes formatted by `opts.formatImagePath` — sessions emit an `@<path>` mention (so Claude reads the image), console shells get the raw path quoted-if-spaced. Because the Claude CLI turns on terminal mouse reporting, a plain mouse drag is forwarded to the app instead of selecting; **hold Shift while dragging** to force a local selection (xterm's built-in override). The git pane re-fetches on a 3s focus-gated poll, on **window focus** (so changes made while the app was in the background show without waiting for the next tick), on every **`session-meta`** event (debounced — a session just touched the working tree, so the Changes list refreshes within a beat instead of up to 3s later), after stage/unstage, and after open-folder (there is no manual refresh button — `git-fetch`/`git-pull` and the branch selector cover the remaining needs). A top `#session-bar` shows the active session's name on the left and, on the right, a two-click **Revert** button (`revert-session` — de-applies just this session's edits, see [Per-session revert](#per-session-revert)) a commit button that calls `commit-session` for that session only, and a **Diff** button carrying a green `+added`/red `-removed` badge that opens a floating diff dialog over the live terminal (see [Per-session diff](#per-session-diff)); `updateSessionBar()` refreshes them on select (also disarming Revert) and on each `session-meta` event. The commit button shows `Commit N files` from the session's **committable** count (`s.diffStat.files` — the same filtered count the commit and the Diff badge use, not the raw tracked-file count, so it never overcounts empty-patch files), or a disabled `Nothing to commit` when that count is zero (see [Per-session commit](#per-session-commit)). While a commit is in flight, the button shows a disabled `Writing commit message…` spinner (the per-session `s.committing` flag, so only *this* session's button blocks — the rest of the app stays live) until the Haiku message call resolves and the commit lands. The Diff button disables when the session changed nothing. The name prefers the Haiku-generated title from `session-name` (which also updates the sidebar row label), falling back to the first prompt, then `session <id8>`. Clicking a git row calls `openFile()`, which routes by extension: images/audio open `#asset-view` (see Asset viewer), everything else opens `#diff-view` over the center pane — `renderDiff()` parses git's unified diff into coloured rows with old/new line-number gutters. Both overlays share `closeOverlay()`; selecting a session also hides them. Each row's status badge carries a tooltip from `statusLabel()` (`renderer/shared/git-status.js`, unit-tested) that spells out which column it's in, because the same porcelain letter reads differently across the staged/unstaged split — most confusingly `D`: a **staged deletion** vs a **file deleted on disk but not staged**. Unstaging a staged deletion does *not* clear it — `git reset` restores the file's index entry but leaves it gone on disk, so git re-reports it as an unstaged deletion and the same red `D` just hops to the Changes list; the tooltip points an unstaged deletion at Discard, which is what actually restores the file. Each row also has a two-click discard (rotate-ccw icon) button left of stage/unstage — the first click arms it red, the second calls `git-revert`. Every two-click "arm then confirm" button across the app (this discard, discard-all, the History tab's revert, the session-bar Revert, the archived-session permanent **Delete**, and the inline browser's **Terminate**) also pops a small speech-bubble callout next to the button on the first click prompting the second — the shared `renderer/shared/arm-hint.js` (`showArmHint(anchor, message?)` / `hideArmHint()`, styled by `styles/arm-hint.css` with the same popover palette as the branch menu) surfaces the otherwise-hidden two-click contract without the user having to discover the tooltip on hover. The bubble shows a short generic "Click again to approve" (`armHint.clickAgain`, two lines) by default; the per-button `armHint.*` keys stay as the buttons' detailed `title` tooltips. It auto-fades, retargets when another button arms, tracks the button on scroll, and is dismissed when the action fires or the button disarms (e.g. a git refresh rebuilds the rows). (The archived-session Delete previously deleted on a single click — it now arms first, matching the rest.) The "Changes" header carries matching stage-all (`+`) and discard-all (same rotate-ccw icon, same two-click arm) buttons that loop those calls over every unstaged file. Stage/unstage/discard now surface a failed result through the shared git-error dialog instead of silently no-op'ing. When `gitStatus` returns conflicts, a **Conflicts** section (`#conflicts-section`, hidden when empty) renders above Staged: each row opens the file's diff and carries a ✓ button that `git add`s it once the markers are resolved; it moves out of the section on the next refresh. The section header also carries a **"Let Claude resolve"** button (`#conflicts-resolve`) that hands the whole resolution to a fresh Claude session (see [Hand a merge to Claude](#hand-a-merge-to-claude)). The commit box has **Commit** (calls `git-commit`: commits the staged changes, but if nothing is staged it first runs `git add -A` so a bare click behaves like "commit all"; clicking with an **empty** message box has main author one from the staged diff via Haiku — the button disables and shows `Writing commit message…` during the call, then reports `Committed: <subject>`), **Undo** (calls `git-undo` → `git reset --soft HEAD~1`, keeping the changes staged), and **Push** (calls `git-push`, then re-refreshes; a push the remote rejected because it diverged comes back `needsMerge` and offers a Claude session instead of the error dialog — see [Hand a merge to Claude](#hand-a-merge-to-claude)). The Push button carries a green pill **ahead badge** pinned to its top-right corner (`#git-ahead`) showing the number of unpushed commits — `gitStatus` returns this as `ahead` (`git rev-list --count @{u}..HEAD`, `0` when there is no upstream or no commits yet), and `refreshGit()` hides the badge when the count is zero. The **Pull** button (pane header) mirrors this with its own green **behind badge** (`#git-behind`) showing how many commits a pull would bring in — `gitStatus` returns this as `behind` (`git rev-list --count HEAD..@{u}`, reflecting the last fetch's view of the remote, so it populates after a fetch/poll); while pulling, the button shows the same sweeping progress bar as Push (`.loading`) and disables; a pull that can't fast-forward comes back `needsMerge` and offers a Claude session (see [Hand a merge to Claude](#hand-a-merge-to-claude)). Toolbar/action icons are inline Lucide SVGs chosen to read distinctly: discard = `rotate-ccw` (single arrow), undo = `undo-2` (hook arrow), branch = `git-branch`.

**Branch selector.** The git pane head carries a `#git-branch` button showing the current branch name (a `git-branch` icon + `#git-branch-name`, refreshed from `gitStatus().branch` on every `refreshGit()`). Clicking it toggles `#branch-menu`, a popover anchored to the head (`#git-main` is the positioning context) holding a `#branch-search` box and a scrolling `#branch-list`. Opening it fetches `git-branches` fresh (so new branches appear), renders local branches most-recent-first with the current one marked, and the search box substring-filters the list live. The list scrolls past ~15 rows (`max-height`), so a repo with many branches stays usable. Clicking a branch calls `git-checkout` and re-refreshes (errors surface through the shared git-error dialog); the popover dismisses on outside click or Escape. The search box doubles as a **create** field: whenever the typed name isn't already an exact branch, a `+`-prefixed "Create branch" row (`.branch-create`) appears at the top of the list — clicking it, or pressing Enter on a non-matching name, calls `git-create-branch` (`git checkout -b <name>` off the current HEAD) and re-refreshes; pressing Enter on a name that matches an existing branch switches to it instead.

The git pane has two tabs (`#git-tabs`, styled like the session filter tabs): **Changes** (the staged/unstaged lists + commit box, all wrapped in `#git-changes-view`) and **History** (`#git-history-view`). Switching to History calls `refreshHistory()` → `git-log`, caching the commits in `allCommits` and rendering one row per commit (subject + `short · author · relDate` meta line) via `renderHistory()`. A `#history-search` box atop the list filters those cached commits live (no re-fetch): every whitespace-split term must appear in a commit's subject, author, or hash (full or short), case-insensitively — the same rule as the unit-tested `filterCommits()` in `git-parse.js`, replicated in the renderer since it can't `require` main. Clicking a row opens that commit's full patch in the diff overlay via `openCommit()` → `showCommit()` (`renderDiff` with its `fileHeaders` flag on, so each `diff --git` emits a file-name header row and re-derives the highlight language from that path). Each commit's `pushed` flag (from `git-log`'s `markPushed()`) drives both its look and its action: an **unpushed** commit gets a green left stripe and a green `Unpushed` pill (matching the green Push ahead badge — the same set of commits) and its button **undoes** it (`git-undo-commit`, a history rewrite), while a **pushed** commit has neither marker and its button **reverts** it (`git-revert-commit`, a new undo commit on top — pushed history can't be safely rewritten). Either way it's the same two-click rotate-ccw arm-then-confirm as the discard buttons (arm message `armHint.undoCommit` vs `armHint.revertCommit`); on failure it surfaces the shared git-error dialog (retitled per call via `#git-error-title`). `refreshHistory()` also runs after a commit/undo so the log stays current; it is **not** on the 3s poll (which only refreshes the Changes lists).

## Welcome screen

The center pane's largest blank surface — shown whenever no session, file, or overlay is open — carries a **welcome watermark** (`#empty-hint` in `index.html`) instead of a bare line of text. `selectFirstVisible()` (sessions) and `closeOverlay()` (`viewer/center.js`) toggle it with `style.display = 'flex'` (the stylesheet centers it), so the `.welcome` card flex-centers and replays a one-shot `welcome-in` fade (gated behind `prefers-reduced-motion`). Its background is a pure-CSS pattern (`#empty-hint::before`): a faint dot field — the app's own status-dot vocabulary scattered as texture — under a soft `--accent` glow, masked to fade at the edges; it's theme-aware via the palette vars (`color-mix`), no image asset. The card holds a three-dot wordmark (a cluster of `--green`/`--yellow`/`--purple` dots — the product is *many parallel sessions*), a tagline, a **Start** action list, and a **Session status** legend mapping each dot colour to its meaning. The three actions are real entry points wired in `renderer/index.js`: **New session** (`newSession`, exported from `sessions.js`), **Go to file** (`open`, exported from `quick-open.js` — same as Ctrl/Cmd+P), and **Open folder** (`browseForFolder`). All copy is i18n'd (`session.welcome*` / `session.state*` keys); the legend strings double as the canonical status labels.

## File explorer

The left sidebar is split 50/50 by default: the sessions list on top, a file tree (`#file-tree`) below; a drag-gutter between them resizes the split (see [Resizable panes](#resizable-panes)). The tree is **lazy** — `loadDir(rel, container, depth)` fetches one directory level over `list-dir` (main reads `repoPath/<rel>` and returns `{name, dir}` entries, folders first then alphabetical, `.git` hidden) and only loads a folder's children the first time it's expanded (▸/▾ twisty). Clicking a row selects it (`select(row, rel, dir)` records the last-clicked item in `selected`, for files and folders alike); clicking a file also calls `openFromTree()`, which reuses the git pane's viewers: images/audio → `showAsset()`, everything else → `showFile()` — the **file editor** (see [File editor](#file-editor)), which fetches the file's text over `read-text` and mounts an editable view in the `#diff-view` container (binary files, sniffed by a NUL byte, show `(binary file)`). The tree refreshes on startup, on **Open folder** (button at the pane's bottom), after any in-app file op, and **automatically when the repo changes on disk** — there's no manual refresh button. `explorer.js` keeps one recursive `fs.watch` over `repoPath` (re-pointed on **Open folder** via `onRepoChange`); each event whose top path segment is a skipped dir (`shouldSkipDir`: `node_modules`/`.git`/build output) is dropped, and the rest are debounced (250ms, trailing-edge so the last event in a burst always fires) into a single `tree-changed` push to the renderer, which calls `refreshTree()`. `refreshTree()` **preserves expansion**: before rebuilding it snapshots which folders are open (rows whose twisty is `▾`, keyed by the `data-rel` it stamps on every row), reloads the root, and re-expands those paths recursively, then re-applies the selection highlight — so a refresh (whether triggered by an on-disk change or by creating/renaming/deleting a file) leaves the tree looking unchanged apart from the edit, and (since it only re-fetches expanded folders) stays cheap. The header icons: **+** (`#files-new`) prompts for a filename (with extension, e.g. `run.py`) through the shared `promptText` modal (`renderer/shared/prompt.js`), then creates the file over `create-file` (main joins it under `repoPath`, refuses paths escaping the root, `mkdir -p`s the parent, writes with the `wx` flag so it fails if the file already exists, and re-prompts with the error otherwise). A **folder+** (`#files-new-folder`) icon sits beside it: it prompts for a folder name and creates it over `create-folder` (same path guards; refuses to clobber an existing entry; `mkdir -p`) via the parallel `createFolderIn(dir)` loop, then refreshes the tree. Next comes a `chevrons-up` **Collapse all** button (`collapseAll`, which just flips every open `▾` twisty back to `▸`). The prompt-and-create loop is `createFileIn(dir)` (re-prompts on error, opens the file on success); `dirOf(rel, dir)` computes a tree item's destination dir — inside it when it's a folder, alongside it (its parent) when it's a file — and `targetDir()` applies that to the toolbar button's `selected`, falling back to the repo root when nothing is selected.

**Context menu & file ops.** Right-clicking a tree row (`contextmenu`) selects it and opens a single floating `#tree-ctx-menu` (built once in `tree.js`, positioned at the cursor and nudged back inside the viewport, dismissed by any outside click or Escape). Its items, each with a leading glyph icon: **New file**, **New folder**, a separator, **Rename**, **Add to chat**, **Copy path**, **Show in file browser**, a separator, then a red **Delete** (`.ctx-danger`). The opener is also exported as `showTreeContextMenu(ev, rel, dir)` so the search-results list can raise the same menu on its (always-file) rows. **New file** runs the shared `createFileIn(dirOf(rel, dir))` against the right-clicked row — so it lands inside a folder or alongside a file (the same flow as the toolbar **+** button); **New folder** does the same through `createFolderIn`. Rename prompts for the new name through `promptText` (pre-filled, **Rename** confirm button) and calls `rename-file` (main `renameSync`, both old/new paths guarded against escaping `repoPath`, refuses to clobber an existing name). Delete confirms through `confirmDialog` (danger variant) and calls `delete-file`, which moves the target to the OS Recycle Bin via `shell.trashItem` (recoverable, not a permanent unlink); failures (rename or delete) report through `confirmDialog` as a one-button notice. Both refresh the tree afterward (expansion preserved, per above) and fix up `selected`. **Copy path** writes the repo-relative path with `navigator.clipboard`; **Add to chat** sends a Claude `@<rel>` mention into the active session via `sendToActiveSession()` (exported from `sessions.js`, writes to the session PTY and refocuses it). **Show in file browser** calls `reveal-in-folder`, which guards the path against escaping `repoPath` and opens the OS file browser with the item selected via `shell.showItemInFolder`.

**Drag to terminal.** Tree **file** rows are `draggable` and put `@<rel>` on the drag's `text/plain`. `#terminal-host` accepts the drop (`dragover` sets `dropEffect = 'copy'`; `drop` reads `text/plain` and feeds it to `sendToActiveSession()`), so dragging a file into the terminal inserts its `@<rel>` mention into the active Claude session — the same text the **Add to chat** menu item sends. The same drop target also accepts **OS file drops** (from Explorer/Finder): those carry no `text/plain`, so the handler falls back to `dataTransfer.files`, resolves each to an absolute path via `api.pathForFile()` (preload-exposed `webUtils.getPathForFile`, since Electron 31 dropped `File.path`), and sends them as space-separated `@<abs>` mentions. A `#file-search` box atop the pane (debounced 150ms) replaces the tree with a `#search-results` list while it has text: **Files** come first from `search-names` (an async recursive walk in main, skipping `.git`/`node_modules`, case-insensitive and NFC-normalised (so a precomposed query matches a filename a macOS volume hands back decomposed), capped at 500 — the query is split on whitespace into terms that **all** must match the file's full repo-relative path, so `enemy .mp3` finds files whose path contains both `enemy` and the `.mp3` extension (folder names count too); a `*.png`/`.png` term matches by extension, any other term is a substring of the path, so a bare `png`/`js` gathers that file type too), then a **References** section is filled in asynchronously from `search-refs` (`git grep -n -I -F -i --untracked`, a subprocess so it never blocks the UI; capped at 500) — the filename results render immediately and don't wait on it. A run-token guards against a slow refs response landing after the query changed. Clicking a result opens it via `openFromTree()`; a reference result also passes `{ line, term }` so `showFile()` calls `jumpToLine()` to centre that line in the editor and select the matched word in the textarea. Right-clicking a result raises the same context menu as the tree (via `showTreeContextMenu`), treating the hit as a file. The opened folder isn't labelled in the sidebar; instead `setWindowTitle(repoPath)` sets the OS window title to `IDE / <project name>` (the path's basename), updated on startup (via `refreshGit`) and on **Open folder**.

## Quick Open

A keyboard-driven fuzzy file switcher (`quick-open.js`), the IDE staple for jumping to a file without the tree. **Ctrl/Cmd+P** from anywhere opens a centered palette — the listener is **capture-phase** (like [terminal links](#terminal-links--inline-browser)) so a focused xterm terminal can't swallow the key first, and it `preventDefault`s the browser's own print binding. On open it fetches the repo's flat file list **once** over `list-files` (a main-process async walk in `explorer.js`, same `.git`/`node_modules` skip set as `search-names` but unfiltered, capped at 10k) and caches it for that session of the palette, then fuzzy-matches **client-side on every keystroke** — no per-keystroke IPC, so typing is instant.

The matcher is `renderer/shared/fuzzy.js`, kept DOM-free so it's unit-tested the same way as `git-status.js` (see [testing.md](testing.md)). `fuzzyMatch(query, target)` requires the query to be a subsequence of the path (case-insensitive via the shared `text-fold.js` `fold()`, so non-ASCII paths match regardless of case; whitespace ignored) and scores it by what makes a guess feel right: runs of consecutive characters, matches on a word/path boundary (a separator, `.`/`_`/`-`, or a camelCase hump), and matches inside the **basename** rather than a parent dir; ties break toward the shorter path and earlier first match. It returns the matched indices so the palette can **bold** them. `fuzzyFilter(query, items, limit)` ranks the whole list and keeps the top 100. ↑/↓ move the highlight (wrapping, `scrollIntoView`), Enter opens the highlighted file through the same `openFromTree()` the tree and search use, and Esc / backdrop click dismiss. The list shows `quickOpen.noMatches` when nothing matches and `quickOpen.empty` when the repo has no files.

## Command Palette

The action sibling of Quick Open (`command-palette.js`): where Quick Open jumps to files, this runs app commands. **Ctrl/Cmd+Shift+P** from anywhere opens the same centered palette shell (it reuses the `.qo*` overlay styles, plus `.cmdp-row`/`.cmdp-title`), with the listener again **capture-phase** so a focused terminal can't swallow it. The command set is registered **once** from `renderer/index.js` — the only place every action function (`newSession`, Quick Open's `open`, `browseForFolder`, `cycleTheme`, `refreshGit`, `refreshTree`, opening Settings) is in scope — via `registerCommands([{ id, titleKey, keywordsKey?, run }, …])`. Commands carry i18n **keys**, not resolved strings, so the palette re-localizes every title each time it opens; switching language in Settings is reflected with no re-registration. `cycleTheme()` (exported from `settings.js`) advances the theme in the `THEMES` registry and persists it, so "Color Theme: Next" flips themes without opening Settings.

The matcher is `renderer/shared/command-match.js`, DOM-free and unit-tested like `fuzzy.js`. `matchCommands(query, commands, limit)` reuses `fuzzyMatch` against each command's **visible title** first (a title hit supplies the highlight positions and ranks by its own score); when the title misses, it falls back to the command's hidden **keywords** (synonyms like "preferences" → Settings), scored below any title hit (a fixed `KEYWORD_PENALTY`) and without highlights. An empty query lists every command in registration order. ↑/↓ move the highlight, Enter runs the highlighted command (errors are caught and logged, never thrown into the palette), and Esc / backdrop dismiss; the list shows `command.noMatches` when nothing matches.

## File editor

Opening any text file (`showFile()` in `viewer/file.js`) mounts an **always-editable** IDE-style editor in the shared `#diff-view` container — there is no separate read/edit mode. A transparent `<textarea>` (`.editor-ta`) overlays a syntax-highlighted `<pre>` (`.editor-pre`) pixel-for-pixel: the textarea owns native editing (caret, selection, undo/redo), its text is `color: transparent` with a visible `caret-color`, and the highlight behind it shows through. A line-number gutter (`.editor-gutter`) sits left and is `position: sticky; left: 0` so it stays pinned during horizontal scroll. On every `input`, `paint()` re-highlights the whole text (capped at 5000 lines, then plain) and rebuilds the gutter so the overlay stays aligned.

**Edits live in memory until saved.** A `buffers` `Map<file, {value, saved}>` holds each file's current text and last-known on-disk content; `dirty = value !== saved`. Switching files or selecting a session never writes to disk and never discards the buffer — reopening a file restores its unsaved edits. A non-dirty buffer is re-synced from disk on open, so external changes (e.g. a Claude session editing the file) show up. **Save** (the `#diff-save` button in the head, or **Ctrl/Cmd+S**) writes `value` over `write-text` (main guards the path against escaping the repo), sets `saved = value`, and calls `refreshGit()` so the new diff appears immediately. The button is disabled when clean, accented when dirty, and the title shows a `•` while unsaved; it is shown only while a file is mounted (`hideDiff()` retires it, so the diff view never shows it). `Tab` inserts a tab and `Enter` carries the current line's leading indent, both via `document.execCommand('insertText', …)` to preserve the native undo stack.

**Find in file.** **Ctrl/Cmd+F** while a file is mounted opens the floating `#editor-find` bar (query box, `current / total` count, match-case toggle, prev/next, close). The pure matcher is `renderer/shared/find.js` — `findMatches(text, query, caseSensitive)` returns all non-overlapping `{start,end}` ranges, `nearestMatch()` picks the hit at/after the caret on open, and `stepMatch()` walks them with wraparound; all three are unit-tested (`test/find.test.mjs`). Case-insensitive matching runs through the shared **`renderer/shared/text-fold.js`** `fold()` (used by every search surface), which is length-preserving so a folded offset is a valid offset into the raw text — the `<mark>` ranges line up even past a Turkish "İ" (whose plain `toLowerCase` is two code units and used to shift every later highlight); it also folds "İ"/"I" → "i" so a lowercase query like `istanbul` finds `İstanbul`. Crucially the matches are **not** shown via the textarea's selection (the next keystroke would overwrite a selected range). Instead a third layer — the `.editor-hl` div, mounted behind the `<pre>` (`z-index` 0 < pre 1 < textarea 2) — mirrors the full text with each hit wrapped in a `<mark>` (current hit brighter); because it shares the `<pre>`'s text, font, padding, `tab-size`, and `white-space: pre`, the marks line up exactly with the rendered code without measuring glyph widths. `showCurrentMatch()` repaints the marks, parks a **collapsed** caret on the current hit (so closing the bar lands there with nothing selected), and scrolls it to centre via `scrollOffsetIntoView()`. Enter / Shift+Enter (or the ↑/↓ buttons) step while focus stays in the query box; Esc / × close. `Alt+C` toggles match-case. The same overlay backs **reference-search jumps**: `jumpToLine(line, term)` (called when a `search-refs` result is clicked, [File explorer](#file-explorer)) paints a single transient yellow mark on the term and clears it after 2s, replacing the old select-the-term behaviour for the same overwrite reason. `hideDiff()` resets the bar (hidden) and clears the overlay alongside the Save button, so a diff/asset view never carries editor chrome.

## Syntax highlighting

Both the diff (`renderDiff`) and the file editor (`paint`) colour code with **highlight.js** — loaded as the prebuilt browser bundle `@highlightjs/cdn-assets` via plain `<script>`/`<link>` in `index.html` (no bundler), exposing `window.hljs`. The `vs2015` theme stylesheet supplies the `.hljs-*` token colours; rows keep their existing `.diff-code` background, so add/del/hunk colouring still shows through.

`langFor(file)` maps a file extension to an hljs language via `EXT_LANG` (covers python, js/ts, c#, c/c++, rust, go, swift, java, kotlin, ruby, php, bash, sql, json, yaml, xml, css, markdown, objc, …). Two grammars aren't in the common bundle and are loaded as extra self-registering scripts: **dos** (`.cmd`/`.bat`) and **powershell** (`.ps1`). Godot is handled without a dedicated grammar: `.gd` → python (GDScript is python-shaped) and `.import`/`.tscn`/`.tres`/`.cfg`/`.godot` → ini. Unknown extensions return `null` → the file viewer auto-detects (`hljs.highlightAuto`); the diff viewer leaves them plain.

- **File editor** highlights the whole text once, then `hlLines()` splits the HTML into per-line fragments, re-opening any span left open across a newline so each line stays balanced (correct for multi-line strings/comments); the fragments are re-joined with `\n` into the highlight `<pre>`.
- **Diff viewer** highlights each line in isolation via `hlLine()` — a hunk is a fragment with no whole-file context, so multi-line constructs may colour imperfectly; hunk headers (`@@`) stay plain.

## Asset viewer

Binary assets have no meaningful text diff, so clicking one in the git pane (or opening it from the tree) opens `#asset-view` instead. The set of routed extensions lives in `shared/ext.js` (`IMG_EXT` / `AUDIO_EXT` / `MODEL_EXT`, kept disjoint); `viewer/center.js` checks them to route to the asset viewer rather than the text/diff view. The renderer fetches the file's bytes as base64 over `read-asset` (`explorer.js` reads it from `repoPath`, returns `{ base64, mime }`), then picks one of four views by type (image/audio build a `data:` URL; the 3D viewer decodes the base64 to an `ArrayBuffer` itself):

- **Pixel editor** — PNGs under 200×200. Drawn onto a `<canvas>`, blown up by an integer scale; left-drag paints/erases single pixels with the current palette/`<input type="color">` colour (the colour wheel). The −/+ toolbar buttons darken/lighten the selected colour by lerping toward black/white. Mouse wheel changes the integer zoom; middle-button drag pans (via `assetBody` scroll), while a middle click without dragging eyedrops the pixel underneath. Undo/redo (↶/↷ buttons or Ctrl+Z / Ctrl+Y, capped at 50 `ImageData` snapshots) is pushed once per stroke. **Save** sends `canvas.toDataURL()` bytes back over `write-asset` and refreshes git.
- **Image zoom** — any other image. An `<img>` with −/+/reset buttons scaling its width. Left-button drag pans a zoomed image by scrolling `#asset-body` (grab/grabbing cursor); the move/up listeners live on the document so the drag keeps tracking past the image edges and are torn down via the asset view's cleanup hook.
- **Audio player** — `wav`/`ogg`/`mp3`. A fully custom player (the native `<audio controls>` is hidden and driven programmatically): a circular play/pause transport, current/duration readout, a speed-cycle button, and a mute toggle + volume slider. Above it an interactive mirrored bar waveform, decoded once from `AudioContext.decodeAudioData` (base64 → `ArrayBuffer`, no network) and reduced to one normalised peak per bar. The played portion fills with an accent gradient as it plays (driven by a `requestAnimationFrame` loop only while playing); clicking or dragging the waveform seeks, and hovering shows a time bubble. Bars recompute on panel resize via a `ResizeObserver`. The view registers a cleanup that cancels the rAF loop, disconnects the observer, and pauses playback.
- **3D model viewer** — `glb`/`gltf`/`fbx`/`obj`/`usdz`/`stl`/`ply` (`MODEL_EXT`). A [three.js](https://threejs.org) scene (`viewer/asset/model.js`) with `OrbitControls` (drag to orbit, scroll to zoom, right-drag to pan), a hemisphere + key light so an unlit mesh stays readable, and a ground grid. Each format has its own loader (`GLTFLoader`/`FBXLoader`/`OBJLoader`/`STLLoader`/`PLYLoader`/`USDZLoader`); they all reduce to a single `Object3D` that `frameObject()` recentres on the origin and pulls the camera back to fit the bounding sphere (a **Reset view** button re-applies that framing). The bare-geometry formats (STL/PLY) are wrapped in a default `MeshStandardMaterial`, honouring per-vertex colours and flat-shading a normal-less mesh. A **scene-graph outliner** (`buildHierarchy()`, `.model-tree`) floats over the canvas's top-left as a translucent panel: a collapsible tree mirroring the `Object3D` hierarchy (loader-injected lights/cameras filtered out), with a short type tag per node (`M`/`G`/`B`), per-node caret to collapse a branch, an eye toggle for that branch's `visible` flag, and click-to-select that parks the orbit pivot (`controls.target`) on the node's bounding-box centre. The panel header collapses the whole tree. `model.js` is **dynamically imported** the first time a model opens, so three.js (a large dependency) never costs app startup. The module is loaded from `node_modules` via an **import map** in `index.html` (`three` → `three.module.js`, `three/addons/` → `examples/jsm/`); the import map must precede the renderer's module script. Cleanup cancels the render loop, disconnects the `ResizeObserver`, disposes every geometry/material/texture, and force-loses the WebGL context. **Limitation**: a model is loaded from its own bytes alone, so a `.gltf`/`.obj` referencing external `.bin`/`.mtl`/texture files renders without them.

`data:` URLs for `<img>`/`<audio>` require the CSP in `index.html` to allow `img-src`/`media-src 'self' data:`; the 3D loaders create `blob:` URLs for embedded textures, so `img-src`/`connect-src` also allow `blob:`. A strict `script-src 'self'` **blocks an inline import map**, so the CSP whitelists it with a **nonce** (`'nonce-threejsimportmap'`, matched by the import map tag's `nonce` attribute) rather than a content hash — a hash over the block's exact bytes silently breaks whenever an editor reformats it or a CRLF checkout rewrites the line endings, whereas the nonce is content-independent. (A static nonce is safe here: the app is a local `file://` page, so the per-request randomness a nonce normally provides is moot — anyone who can alter the local HTML already has code execution.)

**Open externally.** The asset view's head toolbar (`#asset-tools`) carries an **Open externally** button for every asset type that hands the file to the OS's default program for its extension (a `.glb` in the system 3D viewer, an image in the photo app). It routes through `open-asset-external` (`explorer.js`), which resolves the repo-relative path against `repoPath` and calls `shell.openPath` — distinct from `open-external` (which takes an already-absolute path / URL and backs the terminal-link feature).

## Run toolbar

The native Electron application menu is removed (`Menu.setApplicationMenu(null)` in `index.js`); a full-width **run toolbar** (`#toolbar`) sits at the very top of the window instead (`body` is a flex column: toolbar then the `#app` grid). It surfaces VS Code run configs as buttons: **one per launch config** (`.vscode/launch.json`), a separator, then **one per task** (`.vscode/tasks.json`). Clicking a button runs that config/task in a **git-pane terminal tab** (see [Git-pane consoles](#git-pane-consoles)) and focuses it — there is no in-process debugger.

`run-configs.js` parses both files as **JSONC** (`parseJsonc()` strips `//` and `/* */` comments outside strings, then trailing commas, then `JSON.parse`). `get-run-configs` returns the launch names (configs + compounds) and task labels for the toolbar; `run-config({kind, name})` **re-reads** the file each time (so edits are picked up), finds the entry by name, and returns one or more **run specs** `{command, cwd, env, name}` (a compound yields one per referenced configuration). It builds each command line via:

- **Launch** (`buildLaunchCommand`): `node`/`python` types map to `node`/`python <program> <args>` (honouring `runtimeExecutable`/`runtimeArgs`). On **Windows** an extensionless build-tool wrapper used as `runtimeExecutable` is rewritten to its Windows variant (`winExe`: `mvnw`→`mvnw.cmd`, `gradlew`→`gradlew.bat`) — PowerShell can't run the bare Unix script inline, so without this Windows shell-executes it by file association into an **external** window instead of the in-app terminal (the Spring-Boot-via-`mvnw` case). A `godot` type (which carries `project`/`scene` instead of a `program`) maps to `godot --path <project> [<scene>]` — the engine binary defaults to `godot` on PATH (overridable via `runtimeExecutable`/`editor_path`, since the Godot Tools extension keeps it in a VS Code setting, not launch.json), and a `scene` of `main`/`current`/absent runs the project's main scene while an explicit `res://` path runs that scene; other interpreter types in `TYPE_RUNTIME` (`go`→`go run`, `php`, `ruby`, `perl`, `lua`, …) map to their runtime when a `program` is present; anything with a `runtimeExecutable` or bare `program` falls back to running that. A config with none of these (e.g. a browser or attach config) can't be translated, so `run-config` returns `{ok:false, error}` naming the config and its type, and the renderer shows it in the `#run-error-dialog` modal rather than failing silently in the dev console.
- **Task** (`buildTaskCommand`): the `command` (verbatim for `shell` tasks, which may be a whole command line) followed by its quoted `args`. **Compound tasks** are supported via `resolveTask`: a task's `dependsOn` (a label or array of labels) is resolved recursively (cycles broken by a `seen` set) and combined per `dependsOrder` — `"sequence"` collapses the steps into **one** terminal whose commands are chained so each runs only if the previous succeeded (bash/zsh `&&`; Windows PowerShell 5.1 has no `&&`, so `chainCommands` gates each later step on the `$?` success variable instead, and `stepCommand` prepends a `cd`/`Set-Location` for any step with its own `options.cwd`), while `"parallel"`/`"any"` (the default) yields **one terminal per** referenced task. A compound may also carry its own `command`, which runs after its dependencies. A sequence shares a single terminal, so the steps' `options.env` are merged onto it (last wins) rather than applied per step.

`substVars()` resolves the editor-free VS Code variables (`${workspaceFolder}`, `${workspaceFolderBasename}`, `${workspaceRoot}`, `${cwd}`, `${pathSeparator}`, `${env:NAME}`); other `${…}` placeholders are left as-is (best effort). `cwd`/`env` come from the launch config or the task's `options`. A launch config's **`envFile`** (a `.env` path, e.g. Spring Boot's `dev.env`) is read in `run-configs.js` (file IO, kept out of the pure lib) and parsed by `parseEnvFile` (Electron-free, unit-tested: `KEY=VALUE` lines, `#` comments and blanks skipped, `export ` prefix and matching surrounding quotes stripped); those vars seed the spec's env with the config's inline `env` taking precedence, and a missing file is ignored (the config still launches).

The renderer's `runSpecInConsole(spec)` opens (or **reuses** — matched by `spec.kind` + name) a terminal tab for each spec, spawning its shell in the spec's `cwd`/`env` and running its `command`; relaunching restarts its existing tab with a fresh shell rather than piling up duplicates. `kind` is `'config'` for a `.vscode` run (the default) and `'run'` for an editor "run this file" (see [Run a file](#run-a-file)), so the two never collide on a tab. The renderer rebuilds the toolbar (`loadToolbar()`) on startup, after **Open folder**, and whenever the `.vscode` files change underneath it: `run-configs.js` keeps an `fs.watchFile` (2s poll) on `.vscode/launch.json` and `.vscode/tasks.json` — robust even before the files exist, firing on create/edit/delete — and pushes a `run-configs-changed` event that the renderer answers with `loadToolbar()`. The watch re-points to the new folder on **Open folder** via `repo.js`'s `onRepoChange` listener registry (fired from `setRepoPath`).

## Run a file

The file editor's header carries a **Run split-button** (`#run-split`: a square `#run-btn` Play button joined to a narrower `#run-menu-btn` caret) shown only while a **runnable** source file is mounted. It runs a single source file with a language interpreter — no `.vscode` config needed — reusing the same terminal-tab plumbing as the run toolbar (`runSpecInConsole`, `kind: 'run'`).

The pure language table lives in `runners-lib.js` (Electron-free, unit-tested): `LANGUAGES` maps file extensions to a language (`python`, `node`, `typescript`, `ruby`, `go`, `php`, `perl`, `lua`, `shell`, `powershell`), each carrying the ordered **candidate interpreter binaries** to look for (`python3`/`python`/`py`, …) and a `build(bin, file)` that yields the command parts (most are `[bin, file]`; `go` → `go run`, `powershell` → `-File`, `deno` → `deno run`). `langForFile()` resolves a file's language; `buildRunCommand(lang, bin, file, args)` quotes the parts (shared `quoteArg`) and appends any extra args. Add a language by adding a `LANGUAGES` entry (and an `EXT_LANG` mapping for highlighting).

`runners.js` owns the OS side and IPC. It **probes PATH without executing anything** (`existsOnPath`: walks `PATH`, honouring `PATHEXT` on Windows so a bare `python` matches `python.exe`/`.cmd`/`.bat`) and keeps a **registered-interpreter store** — `interpreters.json` in `instance.js`'s `sharedDataDir` (machine-wide, survives restarts, like `last-folder.txt`), a `langId → absolute exe path` map. `resolveBinary(lang)` prefers a registered path that still exists, else the first candidate found on PATH. IPC: `get-runner-langs` (the languages + each one's registered interpreter, so the renderer knows which extensions show the button), `resolve-runner({file, args})` (→ `{ok, run:{command, cwd, name, kind:'run'}}`, or `{unsupported}` for a non-runnable type, or `{needsInterpreter, lang, bins}` when no interpreter is found), `pick-interpreter({langId})` (a native `showOpenDialog` to register an executable — the "we couldn't find the binaries" recovery path — persisted), and `clear-interpreter({langId})` (forget a registration, falling back to PATH detection).

The renderer's `viewer/run.js` drives the button. `showRunFor(file)` (called by `file.js` after a runnable file mounts) reveals it; `code-render.js` hides `#run-split` on every view show/hide, so switching to a git diff or a binary notice drops it. The **Play** button calls `resolve-runner` and either runs the spec (`runSpecInConsole`) or, on `needsInterpreter`, opens a confirm dialog offering to **Locate…** the interpreter (`pick-interpreter`) then retries. The **caret** opens a small popover (same chrome as the shell-picker menu) showing the current interpreter (registered path, or "auto-detected from PATH") plus **Run with arguments…** (prompts, then runs with the extra args), **Select interpreter…** (register/override), and, when one is registered, **Reset to auto-detected**.

## Resizable panes

Four drag-gutters (`.gutter` divs in `index.html`) let the user resize, **not reorder**, the panes. The column layout is a CSS grid whose left/right tracks are the vars `--left`/`--right` on `#app` (`grid-template-columns: var(--left) 5px minmax(0,1fr) 5px var(--right)`); the center is the remaining `1fr`. The sidebar split is the var `--sess-h` on `#sidebar` (`#sessions-pane`'s flex-basis; `#files-pane` takes the rest). The git pane is itself split vertically: `#git-main` (status/commit) fills the top and `#git-console` takes a `--console-h` flex-basis at the bottom (default `33%`); see [Git-pane console](#git-pane-console).

`panes.js`'s `resizer(gutter, axis, sign, read, write, min, max)` is one generic pointer-drag handler wired four times — left column, right column, sidebar split, git-pane console split. It pointer-captures the gutter, then on move clamps `base + sign*delta` to `[min, max()]` and writes the var. Clamps keep every pane within bounds: columns can't shrink the center below `CENTER_MIN` (200px) or themselves below their mins; the sessions pane stays ≥80px and leaves ≥140px for the explorer; the console stays ≥80px and leaves ≥160px for the git content. `sign` is `-1` for the right and console gutters (their pane is on the far side, so dragging toward it shrinks it). Each move calls `fit()` so the active terminal reflows live, plus `fitConsole()` for the console terminal. Sizes are session-only — not persisted across restarts.

## Git-pane consoles

The bottom of the git pane (`#git-console`) holds a **multi-terminal panel**: a tab strip (`#term-tabs`) over a stack of terminal hosts (`#console-hosts`), each tab owning its own xterm.js `Terminal` + shell PTY. These are independent of the per-session Claude PTYs. In the main process the PTYs live in a `consoles` `Map<id, pty>` (`spawnConsole(id, opts)`); IPC is keyed by id — `term-create` (returns `{id}`), `term-restart` (kill + respawn under the **same** id), `term-input`, `term-resize`, `term-kill`, and the `term-data`/`term-exit` events (both carry `{id}`). `term-shells` lists the shells the user can pick (`availableShells()`: `ps`/`cmd` on Windows, `$SHELL`'s basename otherwise). A terminal can run a command two ways. The run toolbar **types** it — `spawnConsole` writes `command + '\r'` into the shell, deferred until the shell's **first output** (its prompt): writing the instant the PTY spawns races shell startup — zsh in particular hasn't initialised its line editor (ZLE) until it has sourced its rc files, so an early write lands the text but drops the submitting `\r`, leaving the command typed but never run; a timeout backstops a shell that emits nothing. The [setup wizard](#claude-code-setup-gate)'s embedded terminal instead passes `args` — the shell **argv** (`zsh -ilc '<cmd>'`) — so the command runs as a shell argument with no prompt-typing and no ZLE race at all (the more robust path; the typed path remains for the run toolbar, which reuses one interactive shell across runs).

In `consoles.js` each tab is a record in the `consoles` `Map` (`{term, fit, host, tab, name, kind}`). `createConsole(opts)` builds the xterm/host/tab and calls `term-create`; `selectConsole(id)` toggles which host is visible (and the active tab) and focuses it; `closeConsole(id)` kills the PTY, disposes the term, drops the tab, and selects a sibling — recreating a default shell if none remain, so a terminal is always open. `term-exit` (e.g. the user typed `exit`) routes to `closeConsole`. Reused id is what lets `term-restart` swap in a fresh shell while `term-data` keeps routing to the same tab; the matching guard in `spawnConsole`'s `onExit` keeps a replaced PTY from reporting its tab as closed.

**A broken terminal must never crash the IDE.** Like the session PTYs, every console PTY operation that can throw when the shell dies under us is contained: `spawnConsole` wraps `pty.spawn` (a bad shell path / cwd / winpty hiccup reports the tab as exited — the renderer reopens a default shell — instead of throwing), and every `p.write` is guarded — the `term-input` handler swallows a write into a closed PTY, and the deferred command write (`sendCommand`, fired from a `setTimeout`/`onData` callback where a throw would have no caller to catch it and would surface as an uncaught main-process exception) is wrapped too. `resize`/`kill` were already guarded for the close race. So a single hung or dead terminal degrades to just that tab closing, never the whole app.

Tabs are **named**: a manually-created one (the **+** button, `#term-add` — a shell-picker menu when more than one shell is available) takes its shell's name (`cmd`/`ps`); a toolbar-launched one takes the config/task name. Labels are truncated (`truncName`, full name in the tab `title`). The header also carries a **Clear** button (`#term-clear`) that calls `term.clear()` on the active terminal (renderer-only; doesn't touch the shell).

## Terminal links & inline browser

Ctrl+click (Cmd on mac) in a terminal opens what's under the cursor — a web URL in an inline browser, a file path in the explorer's viewer (VS Code's gesture). `registerTerminalLinks(term)` wires each session's xterm via `registerLinkProvider`. The provider **always provides links** (decorated only while the modifier is held) rather than gating `provideLinks` on the key state — xterm only re-queries the provider on mouse movement, so gating there would force the user to press Ctrl *before* hovering and keep the terminal focused. Instead a window-level **capture-phase** `keydown`/`keyup` pair tracks `linkModDown` (capture so a focused panel that stops propagation can't hide the key), the link's `hover`/`leave` callbacks record the currently hovered link, and on any modifier change we flip that link's **live `decorations.underline`/`pointerCursor` setters** (xterm swaps these in once a link is hovered) — so a link lights up the instant Ctrl is held over it, regardless of focus or which happened first. Activation is gated on the modifier (`event.ctrlKey`/`metaKey`) so plain clicks pass through and normal hover/drag-to-select are untouched. `findTerminalLinks(lineText)` scans a row for `http(s)://` URLs first, then path-ish tokens (`PATH_RE`), claiming character ranges so the two never overlap. A path token only counts if it has a separator or a known extension (`looksLikePath` against `PATH_EXT`, built from `EXT_LANG`/`IMG_EXT`/`AUDIO_EXT`); a trailing `:line[:col]` is split off on activation and passed to the viewer's jump.

`openTerminalLink` routes by kind: a URL → `showWeb()`; a path → `resolve-link-path` (main resolves an absolute path as-is, else against `repoPath` = the session cwd, and reports `isFile`/`inRepo`/`rel`), then in-repo files open via `openFromTree(rel, {line})` and anything else goes to the OS through `open-external`. `showWeb()` is a center overlay (`#web-view`, peer of the diff/asset overlays, hidden by the shared `closeOverlay()` and on session select) holding a back/forward/reload bar, an **editable address bar**, an open-externally button, a **⋮ menu**, a **Terminate** button, and an Electron **`<webview>`** — a separate guest process, so the host-page CSP doesn't restrict the loaded site. It requires `webviewTag: true` in the window's `webPreferences`.

**One persistent instance.** There is exactly **one** `<webview>`, reused for every open — the browser is never re-instantiated. Besides Ctrl+click, it's opened from the **browser button** in the top toolbar (`#browser-btn`, left of the settings gear, both pinned to the toolbar's right edge), which calls the center coordinator's `openWeb()` → `web.js`'s `openWeb()`: it just reveals the overlay over the *already-loaded* page (focusing the address bar only when nothing is loaded yet). Crucially, hiding the overlay (session switch, `closeOverlay()`, opening another center view) **no longer unloads the page** — `hideWeb()` only sets `display:none`, so the site keeps running in the background and is shown again as-is on the next open. The toolbar button carries an **`.active`** accent tint whenever a real (non-`about:blank`) page is loaded, so it doubles as a browser-is-alive indicator; `web.js` toggles it from the `did-navigate(-in-page)` events and on terminate. The only thing that unloads the page is the **Terminate** button (`#web-terminate`, ⏻): a destructive action, so it uses the same **two-click arm** confirm as the git pane's discard/revert (armed red on the first click, kills the page — `terminateWeb()` sets the webview back to `about:blank`, clears the active tint — on the second), then returns to the active session via `closeOverlay()`. The terminate/close buttons and the browser button are all wired in the center coordinator (`center.js`), which owns the overlay hand-off.

**Inspect element** (`#web-inspect`, a Lucide pipette/color-picker icon left of the open-externally button) lets the user pick any element in the loaded page and copies a full report of it to the clipboard: page URL, XPath, CSS selector, tag/id/classes, pixel size, the key **computed styles** (margins, padding, border, colors, font, layout) and the element's **outerHTML** (trimmed past ~1.5 KB). The picker must run *inside* the guest process, so `web.js` injects it with `webFrame.executeJavaScript('(' + guestInspector.toString() + ')()', true)`: the injected IIFE draws a fixed-position highlight box + selector label that follow the cursor (capture-phase `mousemove`) and returns a **Promise that stays pending until the user clicks an element** — `executeJavaScript` then resolves the host-side `await` with the picked element's (structured-cloneable) data object, which `formatInspect()` turns into the clipboard text (written via `window.api.clipboardWrite`, reliable under `file://`). The button tints accent (`.armed`) while picking and flashes green (`.copied`) after a copy; Escape, re-clicking the button, or closing the overlay (`hideWeb()`) cancel the pick through a `window.__ideInspectCancel` the guest parks for the host to call (`stopInspect()`).

The webview uses a **persistent partition** (`partition="persist:browser"`), so its cookies and site data live on disk under the instance's `userData` and survive restarts (the `persist:` prefix is what makes a partition on-disk rather than in-memory). The **⋮ menu** is the "clean cookies control": its single **Reset cookies** item confirms (danger `confirmDialog`) then calls `clear-web-data` → `session.fromPartition('persist:browser').clearStorageData({ storages: ['cookies'] })` in main, wiping just that partition's cookies and reloading the page; the app's own session is untouched.

The **address bar** (`#web-url`, an `<input>`) is editable: Enter navigates (`normalizeUrl` keeps a real scheme, else prepends `https://`). It offers **suggestions** from a most-recent-first visited-URL list (`web.history` in `localStorage`, capped at 30): focusing the empty bar shows the latest addresses, typing substring-filters them; a suggestion is chosen on `mousedown` (before the input's blur hides the list). `did-navigate`/`did-navigate-in-page` keep the bar in sync (without clobbering mid-typing) and push each address into the history. The ↗ button hands the current URL to `open-external` (→ `shell.openExternal`).

## Error handling & crash logs

Two layers keep a failure from taking down the app.

**Session errors are non-fatal and visible.** Anything that can go wrong around a session — saving/loading the on-disk snapshot, creating/resuming/closing a PTY, committing/reverting/diffing, or tracking hook activity — must never crash the IDE or vanish into the console. In `sessions.js`, `reportSessionError(context, err)` logs the error and pushes a `session-error` event (`{ context, message }`) to the renderer; errors raised before the renderer exists (e.g. a corrupt session file read during the startup load) are **queued** and flushed when the renderer first calls `get-sessions`. Every session IPC handler is wrapped by `guard(context, fn, fallback)` (invoke handlers) or `guardOn(context, fn)` (fire-and-forget `.on` handlers): a throw becomes a reported warning plus a safe return value (`fallback`), instead of a rejected invoke (an unhandled renderer rejection) or an uncaught main-process exception. `persistSession()`/`persistSessions()` are wrapped whole (they run from a timer and on quit, where a throw would be uncaught), `loadPersistedSessions()` distinguishes a missing store (normal first run) from a corrupt file (reported) and never lets one bad file abort the rest, and the hook server reports a failed `recordSessionActivity()` (but still silently ignores a malformed payload). The renderer listens for `session-error` and shows the shared **warning dialog** — `renderer/shared/warn.js`'s `showWarning(message, title)`, the `#warn-dialog` element: the error text in **red** (`#warn-msg`), an **OK** to dismiss and a **Copy** that writes the full text via `clipboardWrite`. It reuses the error/confirm dialog chrome and is safe to call repeatedly (a new message replaces the open one rather than stacking). All its strings are i18n'd (`warn.*`).

**Real crashes are logged.** `crashlog.js`'s `installCrashLogging()` (wired first in `index.js`) records every `uncaughtException`, `unhandledRejection`, and `render-process-gone`/`child-process-gone` to its own timestamped file under a **`crashlogs/`** folder — next to the executable when packaged, else the project root in dev. It does **not** exit (the app stays usable; `window.js` already reloads a dead renderer); the log is a forensic trail. The filename + body formatting is the pure, unit-tested `crashlog-lib.js` (`crashLogName` makes a sortable, filesystem-safe `crash-<iso>.log`; `formatCrash` writes the kind + time + stack). Writes are synchronous so a log survives even a fatal exception. `crashlogs/` is gitignored.

## preload/

The entire IPC surface (`contextBridge.exposeInMainWorld('api', …)`), split into per-subsystem modules that `preload/index.js` composes. Add a channel to the matching module when wiring any new main↔renderer call. `resolve-link-path` and `open-external` back the terminal link feature above.

## Files

Vanilla JS, no bundler, no test framework. The code is split into per-subsystem modules, not three monolithic files; `index.html` + `styles.css` are the only non-JS UI assets. Within `src/`:

- **`main/`** — the main process, one module per concern, each registering its own `ipcMain` handlers as a load side-effect (`index.js` just requires them in order):
  - `index.js` — app bootstrap: command-line switches, require order, `app` lifecycle.
  - `crashlog.js` + `crashlog-lib.js` — crash logging to `crashlogs/` (see [Error handling & crash logs](#error-handling--crash-logs)). `crashlog-lib.js` is the Electron-free filename/body formatting, unit-tested.
  - `instance.js` — per-instance `userData` profile redirect (see [platform-notes.md](platform-notes.md)); must load first.
  - `window.js` — the `BrowserWindow` + window title. Owns `sendToRenderer(channel, payload)` — the one path every main→renderer push must use: it no-ops when the renderer is gone/crashed and swallows the dispose race, so a dead renderer can't turn a `webContents.send` into an unhandled main-process crash. On `render-process-gone` it reloads the window (throttled) so the UI recovers instead of freezing.
  - `sessions.js` — the PTY manager (Claude sessions), hook activity recording (text edits + the [filesystem-change diff](#tracking-filesystem-changes) for binaries/renames/deletes), session naming, the per-session-file persistence layer (see [Session persistence](#session-persistence)), and the session-error reporting/guards (see [Error handling & crash logs](#error-handling--crash-logs)).
  - `session-persist.js` — the Electron-free snapshot logic backing persistence: `serializeSession`/`deserializeSession`, `sessionBytes`, the 100 MB `enforceLimit` eviction, and `persistedState`; unit-tested (`test/session-persist.test.js`).
  - `hook-server.js` — the localhost hook → status HTTP server (see [status-detection.md](status-detection.md)).
  - `repo.js` — the open repo path, folder picker, last-folder + recent-folders persistence. The Open-folder button is a reverse-combobox: it pops up the last 7 opened projects (`get-recent-folders`) above the button plus a "Browse…" entry; picking a recent one re-points via `open-folder-path`, the dialog via `open-folder`. The capping/dedup logic lives in the Electron-free `recent-folders.js` (`addRecent`), unit-tested (see [testing.md](testing.md)).
  - `git.js` — all `git` porcelain handlers and the shared `git()` helper; the pure stdout parsers (`git-parse.js`: `parsePorcelain`/`parseLog`, plus `filterCommits` for the History search) are split out so they're unit-tested (see [testing.md](testing.md)).
  - `session-commit.js` + `edit-ops.js` + `commit-msg.js` — per-session commit/revert (replay/inverse of recorded ops; see [Per-session commit](#per-session-commit)). `edit-ops.js` (op replay) and `commit-msg.js` (the diff→Haiku message phrasing/cleanup, shared with the git pane) are pure (Electron-free) and unit-tested.
  - `explorer.js` — file-tree, file-search, and file CRUD handlers, plus the terminal clipboard handlers (`clipboard-read`/`clipboard-write`/`paste-image`). `clipboard-lib.js` is the Electron-free `withClipboardRetry()` they use to survive transient Windows clipboard-lock throws, unit-tested (see [testing.md](testing.md)).
  - `run-configs.js` + `run-configs-lib.js` — the run toolbar. `run-configs.js` owns file IO, IPC, and the `.vscode` watcher; `run-configs-lib.js` is the Electron-free JSONC parse + launch/task → command translation, unit-tested (see [testing.md](testing.md)).
  - `runners.js` + `runners-lib.js` — the editor's [run-a-file](#run-a-file) button. `runners.js` owns PATH probing, the registered-interpreter store, and IPC; `runners-lib.js` is the Electron-free language table + command translation, unit-tested (see [testing.md](testing.md)).
  - `consoles.js` — the git-pane terminal PTYs.
  - `claude.js` — the shared `runHaiku()` helper (see [Haiku generation](#haiku-generation)) and `claudeAvailable()`, the first-run install detector (see [Claude Code setup gate](#claude-code-setup-gate)).
  - `claude-install.js` — Electron-free, unit-tested per-platform install commands for the setup gate (see [Claude Code setup gate](#claude-code-setup-gate)).
- **`preload/`** — the `contextBridge` IPC surface, split to mirror the main modules (`index.js` composes them). Add a channel here when wiring any new main↔renderer call.
- **`renderer/`** — pure UI, no Node/OS access. `index.js` is the entry; the rest is grouped by area: `sessions.js`, `claude-setup.js` (the first-run install gate, see [Claude Code setup gate](#claude-code-setup-gate)), `git-pane.js`, `toolbar.js`, `consoles.js`, `panes.js`, `settings.js`, `terminal-links.js`, plus `explorer/` (tree + search), `viewer/` (diff, file editor, the [run-a-file](#run-a-file) button, asset views, web overlay), and `shared/` (highlight, prompt/confirm modals, the warning dialog, terminal, ext maps, bootstrap).
- **`i18n/`** — the translation engine (`index.js`) and per-language `locales/` (see [settings.md](settings.md)).
